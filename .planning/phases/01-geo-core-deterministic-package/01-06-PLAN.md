---
phase: 01-geo-core-deterministic-package
plan: 06
type: execute
wave: 2
depends_on: ["01-00", "01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/__tests__/exports.test.ts
  - packages/core/README.md
  - packages/core/scripts/verify-consumable.mjs
  - packages/core/scripts/verify-consumable.cjs
autonomous: true
requirements: [CORE-06]
must_haves:
  truths:
    - "@geo/core's full public API is importable via ESM `import` AND CJS `require` from the built dist"
    - "The package has zero runtime dependencies (verified against the built manifest)"
    - "All five CORE functions + CITABILITY_WEIGHTS + the Fetcher/FetchResult types are exported from the single barrel"
  artifacts:
    - path: "packages/core/src/__tests__/exports.test.ts"
      provides: "public-surface contract test"
      contains: "checkRobots"
    - path: "packages/core/README.md"
      provides: "consumer usage doc (ESM + CJS + workspace link)"
      min_lines: 20
    - path: "packages/core/scripts/verify-consumable.mjs"
      provides: "ESM import smoke against dist"
  key_links:
    - from: "consumer (ESM import + CJS require)"
      to: "packages/core/dist (exports map)"
      via: "package.json exports resolution"
      pattern: "exports"
---

<objective>
Implement CORE-06: prove `@geo/core` is consumable as a zero-runtime-dependency package by BOTH ESM and CJS consumers, with the complete public surface (all five CORE functions, their result types, `CITABILITY_WEIGHTS`, and the `Fetcher`/`FetchResult` seam) reachable from the single barrel. Run the full suite, build dual output, and smoke-test resolution through the `exports` map. Document consumer wiring for the eventual HOW + service integration.

Purpose: This is the phase gate — it verifies the package is actually importable both ways (the dual-package contract four downstream phases depend on) and that zero runtime deps holds against the real built manifest.
Output: Public-surface contract test, ESM + CJS consumability smoke scripts, README consumer doc, full green suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-geo-core-deterministic-package/01-CONTEXT.md
@.planning/phases/01-geo-core-deterministic-package/01-RESEARCH.md
@.planning/phases/01-geo-core-deterministic-package/01-VALIDATION.md
@packages/core/package.json
@packages/core/src/index.ts

<interfaces>
Public surface to assert (the barrel must export all): `checkRobots`, `generateLlmsTxt`, `validateLlmsTxt`, `getSchemaTemplates`, `validateStructuredData`, `computeCitabilityScore`, `scorePassage`, `detectRendering`, `CITABILITY_WEIGHTS`, `AI_CRAWLERS`, `normalizeUrl`; types `Fetcher`, `FetchResult`, `RobotsResult`, `LlmsTxtResult`, `SchemaTemplateResult`, `StructuredDataValidationResult`, `CitabilityResult`, `RenderingResult`, `CrawlData`, `PageData`, `SchemaType`, `CitabilityWeightKey`, `AiCrawler`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Public-surface contract test + barrel completeness</name>
  <read_first>
    - packages/core/src/index.ts (current barrel)
    - .planning/phases/01-geo-core-deterministic-package/01-VALIDATION.md (CORE-06 row + sampling)
    - .planning/phases/01-geo-core-deterministic-package/01-RESEARCH.md (Pitfall 1 dual-package hazard)
  </read_first>
  <files>packages/core/src/index.ts, packages/core/src/__tests__/exports.test.ts</files>
  <behavior>
    - Test: importing the barrel exposes every runtime export in the `<interfaces>` list as a function/object of the expected typeof (functions are functions, CITABILITY_WEIGHTS is an object summing to 100, AI_CRAWLERS is an array).
    - Test: CITABILITY_WEIGHTS imported here is the same object identity used by computeCitabilityScore's breakdown keys (no drift).
  </behavior>
  <action>
    Audit src/index.ts and ensure every name in the `<interfaces>` list is re-exported (add any missing). Write `src/__tests__/exports.test.ts` asserting the full runtime surface and the weights-sum invariant. This test is the executable definition of the public API contract.
  </action>
  <verify>
    <automated>cd packages/core && bun run test -- --run exports</automated>
  </verify>
  <acceptance_criteria>
    - Every listed runtime export is present and of the correct type.
    - Weights-sum invariant re-asserted at the barrel level.
  </acceptance_criteria>
  <done>Barrel exports the complete public surface; contract test green.</done>
</task>

<task type="auto">
  <name>Task 2: Dual ESM+CJS consumability smoke + zero-dep verification + README</name>
  <read_first>
    - packages/core/package.json (exports map, dependencies must be absent)
    - .planning/phases/01-geo-core-deterministic-package/01-RESEARCH.md (Pattern 1/2 build+workspace, Pitfall 2 type:module vs cjs ext, Pitfall 5 workspace hoisting)
  </read_first>
  <files>packages/core/scripts/verify-consumable.mjs, packages/core/scripts/verify-consumable.cjs, packages/core/README.md</files>
  <action>
    Build first (`bun run build`). Create `scripts/verify-consumable.mjs` that does `import * as geo from "../dist/index.js"` and asserts the presence of checkRobots, computeCitabilityScore, detectRendering, generateLlmsTxt, getSchemaTemplates, CITABILITY_WEIGHTS (exit 1 on any missing). Create `scripts/verify-consumable.cjs` that does `const geo = require("../dist/index.cjs")` and asserts the same. Both prove the `exports` map resolves correctly per condition (Pitfall 2).
    Verify zero runtime deps against the real manifest: assert `packages/core/package.json` has no `dependencies` key (or an empty one). Add this as a guard in one of the smoke scripts or as a grep in verify.
    Write `packages/core/README.md` documenting: install via Bun workspace (`"@geo/core": "workspace:*"`), ESM import example, CJS require example, the zero-runtime-dep guarantee, and the injected-Fetcher contract (caller must supply a `Fetcher`; @geo/core does no network I/O — points forward to Phase 2). Note HOW inline-import wiring is structurally supported but full HOW integration is Phase 7 (per VALIDATION manual-only row).
  </action>
  <verify>
    <automated>cd packages/core && bun run build && node scripts/verify-consumable.mjs && node scripts/verify-consumable.cjs && node -e "const p=require('./package.json'); if(p.dependencies && Object.keys(p.dependencies).length) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `bun run build` emits dist/index.js, dist/index.cjs, dist/index.d.ts, dist/index.d.cts.
    - ESM smoke (verify-consumable.mjs) exits 0; CJS smoke (verify-consumable.cjs) exits 0.
    - Zero-dependencies assertion passes against the built manifest.
    - README documents ESM + CJS + workspace link + Fetcher contract.
  </acceptance_criteria>
  <done>Package resolves through the exports map in both module systems, zero runtime deps verified against the real manifest, consumer doc written.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite phase gate + ROADMAP/REQUIREMENTS checkoff prep</name>
  <read_first>
    - .planning/phases/01-geo-core-deterministic-package/01-VALIDATION.md (Sampling Rate: phase gate = full suite green + build emits ESM+CJS+d.ts)
  </read_first>
  <files>packages/core/README.md</files>
  <action>
    Run the full vitest suite (all modules: robots, llmstxt, schema, citability, rendering, skeleton, exports) and the build. This is the phase gate from VALIDATION.md. Record the green-suite + dual-build result in the README's status section. Do not edit ROADMAP/REQUIREMENTS here (the verifier/orchestrator owns checkoff) — just confirm the gate passes so CORE-01..06 are demonstrably satisfied.
  </action>
  <verify>
    <automated>cd packages/core && bun run test -- --run && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (every CORE-0x module test passes).
    - Build emits all four dist artifacts.
  </acceptance_criteria>
  <done>Full suite green and dual build clean — phase gate satisfied; CORE-01..06 demonstrably shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dual-package resolution | ESM and CJS consumers resolving the same package via exports map |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-X1 | Tampering | dual-package hazard (two module instances) | mitigate | exports-map condition ordering (types first); package exposes only plain constants + JSON-serializable results (D-06) — no instanceof on package types. Smoke verifies both entrypoints resolve. |
| T-01-X2 | Tampering | accidental runtime dependency creep | mitigate | Manifest zero-dependencies assertion run against the built package.json in CI verify. |
</threat_model>

<verification>
- `bun run --cwd packages/core test -- --run` full suite green.
- `bun run --cwd packages/core build` emits ESM + CJS + d.ts + d.cts.
- ESM + CJS consumability smokes exit 0.
- Zero-dependencies assertion passes.
</verification>

<success_criteria>
@geo/core is a zero-runtime-dependency package whose full public API resolves via both `import` and `require`, the complete CORE-01..06 surface is exported and tested, and the full suite + dual build pass the phase gate.
</success_criteria>

<output>
Create `.planning/phases/01-geo-core-deterministic-package/01-06-SUMMARY.md` when done.
</output>
