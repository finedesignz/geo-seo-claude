---
phase: 01-geo-core-deterministic-package
plan: 02
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - packages/core/src/llmstxt.ts
  - packages/core/src/index.ts
  - packages/core/src/__tests__/llmstxt.test.ts
autonomous: true
requirements: [CORE-02]
must_haves:
  truths:
    - "generateLlmsTxt(crawlData) emits a valid llms.txt: # Title, > description, ## Section, - [title](url): desc"
    - "validateLlmsTxt detects missing title / sections / links"
    - "Generation is deterministic for the same crawlData input"
  artifacts:
    - path: "packages/core/src/llmstxt.ts"
      provides: "generateLlmsTxt + validateLlmsTxt + CrawlData type"
      contains: "generateLlmsTxt"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "generateLlmsTxt"
      via: "barrel re-export"
      pattern: "generateLlmsTxt"
---

<objective>
Implement CORE-02: `generateLlmsTxt(crawlData)` produces an llms.txt Markdown string per the llmstxt.org convention, and `validateLlmsTxt(text)` reports presence/validity of required and recommended sections. Port from `scripts/llmstxt_generator.py`.

Purpose: Lets consumers generate and validate llms.txt deterministically from crawl data with no network and no LLM.
Output: Both functions exported from the barrel, vitest-covered against the committed fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-geo-core-deterministic-package/01-CONTEXT.md
@.planning/phases/01-geo-core-deterministic-package/01-RESEARCH.md
@packages/core/src/types.ts
@packages/core/fixtures/llmstxt-valid.txt

<interfaces>
From src/types.ts: `LlmsTxtResult` stub (extend), `errors: string[]` convention.

CrawlData (define in llmstxt.ts, export from barrel): `siteName: string`, `siteUrl: string`, `description?: string`, `pages: Array<{ title: string; url: string; description?: string; section?: string }>`, optional `contact?: { website?: string; email?: string }`.

llms.txt format (RESEARCH Code Examples / llmstxt.org): `# Title` (required), `> description` (recommended), `## Section` headings, list items `- [Title](url): desc`. Only `# Title` is mandatory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port generateLlmsTxt + validateLlmsTxt</name>
  <read_first>
    - scripts/llmstxt_generator.py (generate + validate logic — port source)
    - .planning/phases/01-geo-core-deterministic-package/01-RESEARCH.md (llms.txt Format, Assumption A4: only # Title required)
    - packages/core/src/types.ts
    - packages/core/fixtures/llmstxt-valid.txt
  </read_first>
  <files>packages/core/src/llmstxt.ts, packages/core/src/index.ts, packages/core/src/__tests__/llmstxt.test.ts</files>
  <behavior>
    - Test: `generateLlmsTxt` with siteName + description + two pages in section "Docs" emits a string starting with `# `, containing a `> ` description line, a `## Docs` heading, and `- [Title](https://...)` entries.
    - Test: pages with no `section` fall under a default section (e.g. uncategorized) deterministically.
    - Test: same input → byte-identical output (call twice, assert equal — determinism).
    - Test: `validateLlmsTxt` on the committed valid fixture returns `valid:true`, `errors:[]`.
    - Test: `validateLlmsTxt` on text with no `# Title` returns `valid:false` and an error naming the missing title.
    - Test: `validateLlmsTxt` on title-only text returns `valid:true` (title is the only requirement, A4) but surfaces recommendations (e.g. `warnings` for missing description/sections).
  </behavior>
  <action>
    Define and export `CrawlData` (shape above). Implement `generateLlmsTxt(crawlData: CrawlData): string`: emit `# {siteName}`, then `> {description}` if present, then group `pages` by `section` (stable: preserve first-seen section order, then page order within section) emitting `## {section}` + `- [{title}]({url}): {description}` lines (omit `: desc` when description absent). Append a `## Contact` section if `contact` present. Use only stdlib string building — no template libs.
    Implement `validateLlmsTxt(text: string): LlmsTxtResult` returning `{ valid, hasTitle, hasDescription, sectionCount, linkCount, errors: string[], warnings: string[] }`. `valid` is true iff a `# ` title line exists (A4). Push to `errors` only for the mandatory-title miss; push recommendations (missing description, zero sections, zero links) to `warnings`. No throw.
    Re-export both functions and `CrawlData` from src/index.ts.
  </action>
  <verify>
    <automated>cd packages/core && bun run test -- --run llms</automated>
  </verify>
  <acceptance_criteria>
    - Generated output matches the llmstxt.org format markers (`#`, `>`, `##`, `- [..](..)`).
    - Determinism test passes (identical output for identical input).
    - validateLlmsTxt distinguishes mandatory (title → error) from recommended (→ warning).
    - Both functions importable from the barrel.
  </acceptance_criteria>
  <done>generateLlmsTxt + validateLlmsTxt ported, deterministic, format-correct, exported, vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller crawlData → generator | Caller-supplied page titles/urls/descriptions interpolated into Markdown |
| llms.txt text → validator | Untrusted text scanned line-by-line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-L1 | DoS | validateLlmsTxt line scan | mitigate | Line-split scanning, no backtracking-prone regex; bounded work per line. |
| T-01-L2 | Tampering | page url interpolation | accept | Output is a generated text artifact, not executed; caller validates urls upstream. No injection sink in @geo/core. |
</threat_model>

<verification>
- `bun run --cwd packages/core test -- --run llms` green.
- Determinism + format-marker assertions pass.
</verification>

<success_criteria>
generateLlmsTxt yields a spec-shaped, deterministic llms.txt and validateLlmsTxt correctly grades a fixture, all without network or LLM.
</success_criteria>

<output>
Create `.planning/phases/01-geo-core-deterministic-package/01-02-SUMMARY.md` when done.
</output>
