---
phase: 01-geo-core-deterministic-package
plan: 05
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - packages/core/src/rendering.ts
  - packages/core/src/index.ts
  - packages/core/src/__tests__/rendering.test.ts
autonomous: true
requirements: [CORE-05]
must_haves:
  truths:
    - "detectRendering classifies raw HTML as ssr | csr | hybrid with a confidence and signals list"
    - "detectRendering returns csr for the CSR fixture and ssr for the SSR fixture"
    - "Multi-signal heuristic fixes the Python single-threshold bug (large-copy SPA shells still flagged csr)"
  artifacts:
    - path: "packages/core/src/rendering.ts"
      provides: "detectRendering(html)"
      contains: "detectRendering"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "detectRendering"
      via: "barrel re-export"
      pattern: "detectRendering"
---

<objective>
Implement CORE-05: `detectRendering(html)` classifies a page as SSR / CSR / hybrid from raw HTML using a multi-signal heuristic (rendered-text ratio, script-bundle weight, framework hydration markers, framework root divs). Port and improve `scripts/fetch_page.py` SSR detection, fixing the brittle single-boolean threshold bug.

Purpose: Deterministic rendering classification consumers need to know whether a page's content is in the initial HTML (relevant to AI crawlability).
Output: `detectRendering` exported from the barrel, vitest-covered against SSR and CSR fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-geo-core-deterministic-package/01-CONTEXT.md
@.planning/phases/01-geo-core-deterministic-package/01-RESEARCH.md
@packages/core/src/types.ts
@packages/core/fixtures/ssr-page.html
@packages/core/fixtures/csr-page.html

<interfaces>
From src/types.ts: `RenderingResult` stub (extend), `errors: string[]` convention.

RenderingResult (D-08): `rendering: "ssr" | "csr" | "hybrid"`, `confidence: number` (0–1), `signals: string[]`, `wordCount: number`, `scriptCount: number`, `errors: string[]`.

HYDRATION_MARKERS (RESEARCH Pattern 5): `__NEXT_DATA__`, `data-reactroot`, `ng-version`, `__NUXT_DATA__`, `data-server-rendered`, `__svelte`, `x-data`, `v-cloak`. FRAMEWORK_ROOT_PATTERN: `/id=["'](?:app|root|__next|__nuxt)["']/i`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port + improve detectRendering (multi-signal)</name>
  <read_first>
    - scripts/fetch_page.py lines 131-200 (SSR detection: framework root divs, root-text length, word count, has_ssr_content threshold — the bug source)
    - .planning/phases/01-geo-core-deterministic-package/01-RESEARCH.md (Pattern 5 SSR/CSR detection ported+fixed, Pitfall 3 extract structured data before stripping scripts, State of the Art: multi-signal not single boolean)
    - packages/core/src/types.ts
    - packages/core/fixtures/csr-page.html, packages/core/fixtures/ssr-page.html
  </read_first>
  <files>packages/core/src/rendering.ts, packages/core/src/index.ts, packages/core/src/__tests__/rendering.test.ts</files>
  <behavior>
    - Test: `detectRendering` on the CSR fixture (empty `<div id="root"></div>` + heavy script bundle + hydration marker) returns `rendering:"csr"`.
    - Test: `detectRendering` on the SSR fixture (full body text, minimal scripts) returns `rendering:"ssr"`.
    - Test: a page with substantial static copy AND a framework root + hydration markers + heavy scripts returns `"csr"` or `"hybrid"` (NOT forced to ssr) — the Python single-threshold bug fix (large copy alone must not mask CSR).
    - Test: `signals` array is non-empty and names the contributing signals (e.g. "hydration-marker:__NEXT_DATA__", "low-root-text", "high-script-count").
    - Test: `confidence` is within [0,1]; `wordCount` and `scriptCount` are reported.
    - Test: empty HTML → returns a result (default classification) with `errors:[]`, no throw.
    - Test: determinism — identical HTML yields identical result.
  </behavior>
  <action>
    Implement `detectRendering(html: string): RenderingResult`. Guard input length (~5MB max) before regex passes. Compute signals: word count of visible text (strip `<script>`/`<style>` then strip tags — but only AFTER any structured-data needs; here scripts are stripped for word count), `scriptCount` via `/<script/gi`, presence of FRAMEWORK_ROOT_PATTERN, matched HYDRATION_MARKERS, and an approximate root-div inner-text length. Score these into a weighted decision: strong CSR signals (empty/low-text framework root + heavy scripts + CSR markers like `v-cloak`) → `csr`; rich initial text + server-render markers (`data-server-rendered`, `data-reactroot` with content) + low scripts → `ssr`; mixed → `hybrid`. Do NOT gate solely on the Python `text_length<50 && wordCount<200` rule — combine signals (fix). Populate `signals[]` with human-readable signal names and set `confidence` from signal agreement. No throw on empty/garbage input.
    Re-export `detectRendering` from src/index.ts.
  </action>
  <verify>
    <automated>cd packages/core && bun run test -- --run rendering</automated>
  </verify>
  <acceptance_criteria>
    - CSR fixture → "csr"; SSR fixture → "ssr".
    - Large-copy-SPA case is not misclassified as ssr (bug-fix assertion).
    - signals non-empty, confidence in [0,1], wordCount/scriptCount reported.
    - Empty input returns a no-error result; determinism test passes.
    - `detectRendering` importable from the barrel.
  </acceptance_criteria>
  <done>detectRendering ported with multi-signal heuristic, single-threshold bug fixed, fixtures classified correctly, exported, vitest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| html → tag/script-stripping regex | Untrusted, possibly adversarial HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-D1 | DoS | script/style/tag-stripping regex on adversarial HTML | mitigate | Lazy `[\s\S]*?` quantifiers; input-length guard (~5MB) before regex. Oversized/decompression handling is caller's (Phase 2) — documented boundary. |
| T-01-D2 | Information disclosure | misclassification | accept | Heuristic, not security-critical; confidence + signals expose uncertainty to caller. |
</threat_model>

<verification>
- `bun run --cwd packages/core test -- --run rendering` green.
- Fixture classification + bug-fix + determinism assertions pass.
</verification>

<success_criteria>
detectRendering deterministically classifies SSR/CSR/hybrid with confidence and signals from raw HTML, zero deps, no throw, and fixes the Python single-threshold false-positive.
</success_criteria>

<output>
Create `.planning/phases/01-geo-core-deterministic-package/01-05-SUMMARY.md` when done.
</output>
