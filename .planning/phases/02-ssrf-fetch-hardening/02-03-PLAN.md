---
phase: 02-ssrf-fetch-hardening
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/fetch/src/decompression.ts
  - packages/fetch/src/safe-fetcher.ts
  - packages/fetch/src/__tests__/size.test.ts
  - packages/fetch/src/__tests__/decompression.test.ts
  - packages/fetch/src/__tests__/exports.test.ts
autonomous: true
requirements: [SEC-04, SEC-05]
must_haves:
  truths:
    - "A Content-Length header above maxBytes is rejected before the body is read (early reject)"
    - "A streamed body exceeding maxBytes (default ~5MB) aborts and returns RESPONSE_TOO_LARGE — no full body buffered"
    - "A decompression bomb (high-ratio gzip) is capped on DECOMPRESSED bytes and returns DECOMPRESSION_BOMB"
    - "More than 2 stacked Content-Encoding layers returns DECOMPRESSION_BOMB"
    - "The full @geo/fetch public surface (createSafeFetcher, SafeFetcherOptions, FetchErrorCode) is exported and the build emits ESM+CJS+dts"
    - "createSafeFetcher() is assignable to @geo/core's Fetcher type"
  artifacts:
    - path: "packages/fetch/src/decompression.ts"
      provides: "makeByteCounter + buildDecompressChain (gzip/deflate/br) capping decompressed bytes"
      contains: "makeByteCounter"
    - path: "packages/fetch/src/__tests__/exports.test.ts"
      provides: "public-surface + Fetcher-type conformance phase gate"
      contains: "createSafeFetcher"
  key_links:
    - from: "packages/fetch/src/safe-fetcher.ts body handling"
      to: "packages/fetch/src/decompression.ts"
      via: "stream body through decompress + byte counter"
      pattern: "makeByteCounter|buildDecompressChain"
---

<objective>
Final vertical slice + phase gate: stream the response body through a decompression pipeline with a running DECOMPRESSED-byte counter that aborts past maxBytes (default ~5MB, aligned with @geo/core's 5MB parse guard) — covering both the oversize-response and decompression-bomb vectors (SEC-04). Honor Content-Length for early rejection, bound stacked Content-Encoding layers to 2, and return RESPONSE_TOO_LARGE / DECOMPRESSION_BOMB structured codes (SEC-05). Close the phase with an exports/type-conformance gate proving the complete `@geo/fetch` surface and a clean dual build.

Purpose: Unbounded bodies and zip bombs are DoS vectors and would feed oversized input to @geo/core's parsers; this is the last hardening layer.
Output: A fully hardened fetcher (SEC-01..05 all green), dual ESM+CJS build, and a phase-gate test asserting Fetcher conformance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ssrf-fetch-hardening/02-RESEARCH.md
@packages/fetch/src/safe-fetcher.ts
@packages/fetch/src/errors.ts
@packages/fetch/test/helpers/test-server.ts
@packages/core/src/types.ts

<interfaces>
From errors.ts: RESPONSE_TOO_LARGE, DECOMPRESSION_BOMB.
SafeFetcherOptions.maxBytes default 5_000_000 (defined Wave 1).
RESEARCH Pattern 4 (decompression-bomb defense: makeByteCounter Transform + buildDecompressChain) + Pitfall 4 (Content-Encoding stacking).
node:zlib createGunzip/createInflate/createBrotliDecompress; node:stream Transform/pipeline.
@geo/core Fetcher type for the conformance assertion: `export type Fetcher = (url: string) => Promise<FetchResult>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: decompression.ts — byte-counting decompress pipeline</name>
  <files>packages/fetch/src/decompression.ts, packages/fetch/src/__tests__/decompression.test.ts</files>
  <read_first>
    - .planning/phases/02-ssrf-fetch-hardening/02-RESEARCH.md Pattern 4 (makeByteCounter, buildDecompressChain) + Pitfall 4
    - packages/fetch/src/errors.ts (RESPONSE_TOO_LARGE, DECOMPRESSION_BOMB)
  </read_first>
  <behavior>
    makeByteCounter(maxBytes, code): a node:stream Transform that sums chunk byte length and errors with .code = code (RESPONSE_TOO_LARGE or DECOMPRESSION_BOMB) once the running total exceeds maxBytes; otherwise passes the chunk through.
    buildDecompressChain(contentEncoding: string|null, maxBytes): returns the ordered decompressor stages for the body stream:
    - Parse comma-separated encodings (e.g. "gzip", "br", "gzip, gzip").
    - > 2 stages → throw with .code = DECOMPRESSION_BOMB (pathological stacking).
    - Map gzip/x-gzip→createGunzip, deflate→createInflate, br→createBrotliDecompress; unknown encoding → throw FETCH_ERROR.
    - The DECOMPRESSED total (after the final decompressor) is what the byte counter caps.
    Tests (use node:zlib to build fixtures in-test, no network):
    - identity body of N bytes, counter cap N-1 → RESPONSE_TOO_LARGE.
    - small gzip payload that inflates beyond maxBytes (high-ratio: e.g. 1KB of zeros gzipped, cap small) → DECOMPRESSION_BOMB.
    - "gzip, gzip, gzip" (3 layers) → DECOMPRESSION_BOMB before processing.
    - valid single gzip under cap → decompresses to expected bytes, no error.
    - unknown encoding "foo" → FETCH_ERROR.
  </behavior>
  <action>
    Implement makeByteCounter + buildDecompressChain per RESEARCH Pattern 4. Cap is on DECOMPRESSED bytes — place the final counter AFTER the last decompressor. Reject >2 stacked encodings up front. Use node:zlib + node:stream only (zero extra deps). Write decompression.test.ts building gzip/identity fixtures with node:zlib and asserting each behavior. No inline code in summary.
  </action>
  <verify>
    <automated>cd packages/fetch && bun run test -- --run src/__tests__/decompression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - oversize identity → RESPONSE_TOO_LARGE; high-ratio gzip → DECOMPRESSION_BOMB
    - 3 stacked encodings → DECOMPRESSION_BOMB; unknown encoding → FETCH_ERROR
    - valid under-cap gzip decompresses correctly with no error
  </acceptance_criteria>
  <done>Decompressed-byte cap + stacked-encoding bound enforced with structured codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire size cap + decompression into fetcher; exports/Fetcher-conformance phase gate</name>
  <files>packages/fetch/src/safe-fetcher.ts, packages/fetch/src/__tests__/size.test.ts, packages/fetch/src/__tests__/exports.test.ts</files>
  <read_first>
    - packages/fetch/src/safe-fetcher.ts (body-reading section from Wave 1/2 to replace with streamed+counted pipeline)
    - packages/fetch/src/decompression.ts (makeByteCounter, buildDecompressChain)
    - packages/core/src/types.ts (Fetcher type for conformance assertion)
    - packages/core/src/__tests__/exports.test.ts (mirror the exports-gate convention)
    - .planning/phases/02-ssrf-fetch-hardening/02-RESEARCH.md Pattern 4 + Pitfall 4 (Content-Length early reject is necessary but not sufficient)
  </read_first>
  <behavior>
    Replace the Wave 1/2 "read body as text" with a streamed, bounded pipeline on the final (non-redirect) response:
    - Content-Length header present and > maxBytes → buildErrorResult RESPONSE_TOO_LARGE BEFORE reading body (early reject).
    - Otherwise pipe the response body stream through buildDecompressChain(content-encoding, maxBytes) then makeByteCounter(maxBytes, RESPONSE_TOO_LARGE); the decompressor's own counter uses DECOMPRESSION_BOMB. On the pipeline error .code → buildErrorResult with that code (abort the stream, do not buffer the whole body).
    - On success collect the bounded decompressed bytes to a UTF-8 string → FetchResult.body.
    - Content-Length absent/wrong is fine — the streamed counter is authoritative.
    size.test.ts (loopback server):
    - server sends body > maxBytes with correct Content-Length → RESPONSE_TOO_LARGE (early reject; assert minimal bytes read).
    - server sends chunked body (no Content-Length) exceeding maxBytes → RESPONSE_TOO_LARGE via streamed counter.
    - server sends Content-Encoding: gzip high-ratio bomb → DECOMPRESSION_BOMB.
    - server sends Content-Encoding: gzip,gzip,gzip → DECOMPRESSION_BOMB.
    - under-cap body → 200 with correct body string.
    exports.test.ts (phase gate):
    - import { createSafeFetcher, FetchErrorCode } from "../index.js" — assert createSafeFetcher is a function returning a function; assert all ten FetchErrorCode values present.
    - Type conformance: assign createSafeFetcher() to a `const f: import("@geo/core").Fetcher` binding (compile-time; tsc gate). Include a runtime check that the returned promise resolves to an object with url/status/headers/body/redirectChain keys.
  </behavior>
  <action>
    Wire the streamed bounded body pipeline into safe-fetcher.ts's final-response handling (replacing direct text read). Add Content-Length early reject. Pipe through buildDecompressChain + makeByteCounter, aborting on cap. Map pipeline .code to buildErrorResult. Write size.test.ts (loopback server scenarios incl. chunked, gzip bomb, stacked) and exports.test.ts as the phase gate (public surface + Fetcher type conformance). Then run the FULL workspace build + suite. No inline code in summary.
  </action>
  <verify>
    <automated>cd packages/fetch && bun run build && bun run test -- --run && cd ../core && bun run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Oversize (Content-Length AND chunked) → RESPONSE_TOO_LARGE; gzip bomb + 3-stacked → DECOMPRESSION_BOMB; under-cap body returns correct text
    - `bun run --cwd packages/fetch build` emits dist/index.js, dist/index.cjs, dist/index.d.ts
    - exports.test.ts green: createSafeFetcher exported, all ten FetchErrorCode values present, returned fetcher assignable to @geo/core Fetcher
    - `bunx tsc --noEmit -p packages/fetch/tsconfig.json` exits 0
    - Full workspace suite (fetch + core) green
  </acceptance_criteria>
  <done>SEC-04 size + decompression-bomb enforced on streamed body; phase gate proves full surface + Fetcher conformance + dual build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| response body stream → memory | Server-controlled body size/compression (untrusted) can exhaust memory |
| Content-Encoding header → decompressor | Attacker controls encoding stack depth + inflation ratio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Denial of Service | oversize response body | mitigate | Content-Length early reject + streamed byte counter → RESPONSE_TOO_LARGE; test Content-Length AND chunked |
| T-02-15 | Denial of Service | decompression bomb (high inflate ratio) | mitigate | cap DECOMPRESSED bytes via makeByteCounter after decompressor → DECOMPRESSION_BOMB |
| T-02-16 | Denial of Service | stacked Content-Encoding layers | mitigate | reject >2 encodings → DECOMPRESSION_BOMB; test gzip,gzip,gzip |
| T-02-17 | Tampering | spoofed/absent Content-Length | mitigate | streamed counter is authoritative, not Content-Length alone |
</threat_model>

<verification>
- `bun run --cwd packages/fetch build && bun run --cwd packages/fetch test -- --run && bun run --cwd packages/core test -- --run` all green (phase gate)
- `bunx tsc --noEmit -p packages/fetch/tsconfig.json` exits 0 (Fetcher conformance)
- All SEC-01..05 requirement tests green across the package
</verification>

<success_criteria>
- SEC-04: oversize + decompression-bomb (incl. stacked encodings) fail with structured codes on a streamed body
- SEC-05: all failure paths return FetchResult.error codes, never throw
- Phase gate: complete @geo/fetch public surface exported, dual ESM+CJS+dts build, createSafeFetcher conforms to @geo/core Fetcher
</success_criteria>

<output>
Create `.planning/phases/02-ssrf-fetch-hardening/02-03-SUMMARY.md` when done
</output>
