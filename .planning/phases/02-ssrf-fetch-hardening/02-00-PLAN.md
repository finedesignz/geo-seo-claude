---
phase: 02-ssrf-fetch-hardening
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/fetch/package.json
  - packages/fetch/tsconfig.json
  - packages/fetch/tsup.config.ts
  - packages/fetch/vitest.config.ts
  - packages/fetch/src/errors.ts
  - packages/fetch/src/ip-validator.ts
  - packages/fetch/src/index.ts
  - packages/fetch/src/__tests__/ip-validator.test.ts
  - packages/fetch/src/__tests__/skeleton.test.ts
  - packages/fetch/test/helpers/test-server.ts
  - packages/fetch/test/helpers/mock-resolver.ts
autonomous: true
requirements: [SEC-01, SEC-05]
must_haves:
  truths:
    - "`@geo/fetch` builds and its test suite runs under vitest"
    - "isBlockedIP() denies loopback, private, link-local, cloud-metadata, IPv4-mapped-IPv6, octal/hex/decimal-obfuscated IPs and allows public unicast"
    - "FetchErrorCode enum exports all ten codes and buildErrorResult() returns a FetchResult-shaped object with error set and no body"
    - "loopback test-server + mock-resolver helpers exist and are importable by later-wave integration tests"
  artifacts:
    - path: "packages/fetch/src/ip-validator.ts"
      provides: "isBlockedIP(raw) using ipaddr.js with IPv4-mapped unwrap"
      contains: "isBlockedIP"
    - path: "packages/fetch/src/errors.ts"
      provides: "FetchErrorCode enum + buildErrorResult"
      contains: "FetchErrorCode"
    - path: "packages/fetch/src/__tests__/ip-validator.test.ts"
      provides: "CIDR + obfuscation table covering SEC-01"
      min_lines: 40
    - path: "packages/fetch/test/helpers/test-server.ts"
      provides: "loopback http test server factory"
      contains: "createTestServer"
    - path: "packages/fetch/test/helpers/mock-resolver.ts"
      provides: "injectable fake DNS resolver for rebinding tests"
      contains: "createMockResolver"
  key_links:
    - from: "packages/fetch/package.json"
      to: "@geo/core"
      via: "workspace dependency for Fetcher/FetchResult type"
      pattern: "@geo/core"
    - from: "packages/fetch/src/ip-validator.ts"
      to: "ipaddr.js"
      via: "import * as ipaddr"
      pattern: "ipaddr"
---

<objective>
Wave 0 scaffold for `@geo/fetch`: create the workspace package, install `undici` + `ipaddr.js`, wire vitest/tsup mirroring `@geo/core`, and land the two leaf modules that have zero network dependency — the SSRF IP classifier (`ip-validator.ts`, SEC-01) and the structured error model (`errors.ts`, SEC-05) — plus the shared loopback test-server and mock-resolver helpers that every later-wave integration test consumes.

Purpose: Establish deterministic test infrastructure and the two pure-logic pillars (IP deny-list + error codes) before any network code exists, so later waves build the fetcher against proven primitives.
Output: A buildable `packages/fetch/` package, a green IP-validator unit table, a failing-but-present skeleton e2e test, and reusable test helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ssrf-fetch-hardening/02-CONTEXT.md
@.planning/phases/02-ssrf-fetch-hardening/02-RESEARCH.md
@.planning/phases/02-ssrf-fetch-hardening/02-VALIDATION.md

<interfaces>
The fetcher this package builds MUST satisfy @geo/core's contract (from packages/core/src/types.ts):

```typescript
export interface FetchResult {
  url: string;
  status: number;
  headers: Record<string, string>; // lowercase-keyed
  body: string;
  redirectChain: Array<{ url: string; status: number }>;
  error?: string;
}
export type Fetcher = (url: string) => Promise<FetchResult>;
```

Mirror @geo/core package conventions:
- package.json: `"type": "module"`, dual exports import/require, `build: tsup`, `test: vitest`
- tsconfig.json: identical to packages/core/tsconfig.json (target ES2022, moduleResolution bundler, ignoreDeprecations "6.0", strict, noUncheckedIndexedAccess)
- vitest.config.ts: `include: ["src/__tests__/**/*.test.ts"]`, environment node, watch false
- tsup.config.ts: entry src/index.ts, format esm+cjs, dts true
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @geo/fetch package + install deps</name>
  <files>packages/fetch/package.json, packages/fetch/tsconfig.json, packages/fetch/tsup.config.ts, packages/fetch/vitest.config.ts, packages/fetch/src/index.ts</files>
  <read_first>
    - packages/core/package.json (mirror exports/scripts/devDeps shape)
    - packages/core/tsconfig.json (copy verbatim)
    - packages/core/tsup.config.ts and packages/core/vitest.config.ts (copy verbatim)
    - Root package.json (workspaces: ["packages/*"] — new package auto-discovered)
  </read_first>
  <action>
    Create `packages/fetch/` as workspace package `@geo/fetch` version 0.1.0, `"type": "module"`, dual ESM+CJS exports identical in shape to `@geo/core`. Add `dependencies`: `undici` 8.3.0 and `ipaddr.js` 2.4.0 (per RESEARCH Standard Stack — do NOT use the `ip` npm package, it has the octal/null SSRF-bypass CVE). Add `devDependencies` matching core: tsup 8.5.1, vitest 4.1.8, typescript 6.0.3, @types/node 25.9.1. Add `@geo/core` as a `dependencies` entry using workspace protocol (`"@geo/core": "workspace:*"`) for the Fetcher/FetchResult type contract. Copy tsconfig.json, tsup.config.ts, vitest.config.ts verbatim from `packages/core/`. Create a minimal `src/index.ts` that re-exports from `./errors.js` and `./ip-validator.js` (the factory `createSafeFetcher` export is added in Wave 1; leave a typed placeholder export only if needed to compile). Run `bun install` at repo root so the workspace link + deps resolve. Use Bun (this is a Bun workspace per bun.lock); install with `bun install`.
  </action>
  <verify>
    <automated>cd packages/fetch && bun install && bunx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `bun install` at root resolves `@geo/fetch` with `undici`, `ipaddr.js`, and `@geo/core` workspace link present in packages/fetch/node_modules or hoisted root node_modules
    - `bunx tsc --noEmit` in packages/fetch exits 0
    - packages/fetch/package.json declares `undici` and `ipaddr.js` in dependencies and does NOT declare the `ip` package
  </acceptance_criteria>
  <done>Package builds clean, deps installed, @geo/core type import resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: errors.ts + ip-validator.ts with SEC-01 CIDR/obfuscation table + test infra</name>
  <files>packages/fetch/src/errors.ts, packages/fetch/src/ip-validator.ts, packages/fetch/src/__tests__/ip-validator.test.ts, packages/fetch/src/__tests__/skeleton.test.ts, packages/fetch/test/helpers/test-server.ts, packages/fetch/test/helpers/mock-resolver.ts</files>
  <read_first>
    - .planning/phases/02-ssrf-fetch-hardening/02-RESEARCH.md "Code Examples" (isBlockedIP via ipaddr.js, DENY_RANGES) and Pitfalls 2 & 5 (IPv4-mapped IPv6, octal/decimal/hex obfuscation)
    - packages/core/src/types.ts (FetchResult shape buildErrorResult must produce)
    - packages/core/src/__tests__/skeleton.test.ts (existing skeleton-test convention to mirror)
  </read_first>
  <behavior>
    ip-validator (SEC-01) — isBlockedIP(raw: string): boolean, BLOCKED when:
    - "127.0.0.1", "127.255.255.255" (loopback)
    - "10.0.0.1", "172.16.0.1", "192.168.1.1" (private)
    - "169.254.169.254" (cloud metadata, link-local), "169.254.1.1"
    - "0.0.0.0" (unspecified), "255.255.255.255" (broadcast)
    - "::1" (IPv6 loopback), "fc00::1"/"fd00::1" (ULA uniqueLocal), "fe80::1" (link-local), "::" (unspecified)
    - "::ffff:169.254.169.254", "::ffff:127.0.0.1" (IPv4-mapped IPv6 — unwrap via isIPv4MappedAddress()+toIPv4Address() BEFORE range check)
    - "2130706433" (decimal 127.0.0.1), "017700000001" (octal), "0x7f000001" (hex) — ipaddr.parse handles these; assert all three are BLOCKED
    - unparseable garbage ("not-an-ip", "") → BLOCKED (deny by default)
    ALLOWED (returns false): "8.8.8.8", "1.1.1.1" (public unicast), "2606:4700:4700::1111" (public IPv6 unicast).
    Strategy: deny anything whose ipaddr.js range() !== "unicast" (default-deny non-global per D-04). Verify the three Open-Question public IPs return range()==="unicast" in the test.
    errors (SEC-05) — FetchErrorCode enum with exactly: SSRF_BLOCKED_IP, SSRF_BLOCKED_SCHEME, SSRF_BLOCKED_PORT, DNS_RESOLUTION_FAILED, REDIRECT_BLOCKED, TOO_MANY_REDIRECTS, RESPONSE_TOO_LARGE, DECOMPRESSION_BOMB, CONNECT_TIMEOUT, FETCH_ERROR. buildErrorResult(url, code, redirectChain?) returns a FetchResult with status 0, empty headers {}, body "", the given redirectChain (default []), and error set to the code string. NEVER throws.
  </behavior>
  <action>
    Implement `src/errors.ts`: a `FetchErrorCode` const enum/object (string-valued) exporting the ten codes from D-09, plus `buildErrorResult(url, code, redirectChain = [])` returning the no-body FetchResult described in behavior. Implement `src/ip-validator.ts`: `isBlockedIP(raw)` per RESEARCH Code Example — parse with `ipaddr.parse`, if IPv6 and `isIPv4MappedAddress()` unwrap via `toIPv4Address()`, then return `addr.range() !== "unicast"`; wrap in try/catch returning true on parse failure (deny by default). Write `src/__tests__/ip-validator.test.ts` as a table-driven vitest suite covering EVERY case in the behavior block (one assertion per row). Write `src/__tests__/skeleton.test.ts` as a placeholder e2e test for the not-yet-built fetcher: `it.skip` or `it.fails` describing "createSafeFetcher blocks 169.254.169.254" so the contract is recorded before Wave 1 implements it — mark it pending so the suite stays green this wave. Write `test/helpers/test-server.ts` exporting `createTestServer(handler)` (node:http, listen on 127.0.0.1:0, resolve {server, url, port, close()}). Write `test/helpers/mock-resolver.ts` exporting `createMockResolver(sequence)` — a fake that returns queued address arrays on successive calls (for the Wave 1 rebinding test: public IP first call, private IP second call). Do NOT inline code blocks in the summary; these are real source files.
  </action>
  <verify>
    <automated>cd packages/fetch && bun run test -- --run src/__tests__/ip-validator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun run --cwd packages/fetch test -- --run` passes with the full ip-validator table green
    - Test file asserts 169.254.169.254 BLOCKED, ::ffff:169.254.169.254 BLOCKED, 2130706433 BLOCKED, 017700000001 BLOCKED, 0x7f000001 BLOCKED, 8.8.8.8 ALLOWED, 2606:4700:4700::1111 ALLOWED
    - `grep -v '^[[:space:]]*//' packages/fetch/src/errors.ts | grep -c 'SSRF_BLOCKED_IP\|RESPONSE_TOO_LARGE\|DECOMPRESSION_BOMB\|TOO_MANY_REDIRECTS\|REDIRECT_BLOCKED\|CONNECT_TIMEOUT\|DNS_RESOLUTION_FAILED\|SSRF_BLOCKED_SCHEME\|SSRF_BLOCKED_PORT\|FETCH_ERROR'` reports 10
    - test/helpers/test-server.ts exports createTestServer; test/helpers/mock-resolver.ts exports createMockResolver
  </acceptance_criteria>
  <done>IP deny-list table green, all ten error codes present, test helpers importable, skeleton e2e test recorded as pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → @geo/fetch | Caller-supplied URL string (untrusted) crosses into DNS/IP validation |
| @geo/fetch → DNS/network | Resolved IPs (attacker-influenceable) cross into connection layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing/Elevation | ip-validator IPv4-mapped IPv6 | mitigate | isIPv4MappedAddress() unwrap before range() check; test ::ffff:169.254.169.254 |
| T-02-02 | Spoofing | ip-validator octal/hex/decimal IP forms | mitigate | ipaddr.parse normalizes all forms; tests assert 017700000001/0x7f000001/2130706433 blocked; ip npm package banned (CVE) |
| T-02-03 | Info Disclosure | cloud-metadata 169.254.169.254 | mitigate | link-local range deny + explicit unit test |
| T-02-SC | Tampering | bun add undici / ipaddr.js | mitigate | RESEARCH Package Legitimacy Audit: both [OK] approved (Node-bundled / 10yr stable), no human checkpoint needed |
</threat_model>

<verification>
- `bun run --cwd packages/fetch test -- --run` green
- `bunx tsc --noEmit -p packages/fetch/tsconfig.json` exits 0
- No `ip` npm package in dependency tree
</verification>

<success_criteria>
- packages/fetch builds and tests run under vitest
- SEC-01 IP deny-list fully covered by green table incl. obfuscation + IPv4-mapped vectors
- SEC-05 error model (ten codes + buildErrorResult no-body) present and exported
- test-server + mock-resolver helpers exist for Wave 1+ integration tests
</success_criteria>

<output>
Create `.planning/phases/02-ssrf-fetch-hardening/02-00-SUMMARY.md` when done
</output>
