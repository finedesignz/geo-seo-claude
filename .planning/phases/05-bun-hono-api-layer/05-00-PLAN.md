---
phase: 05-bun-hono-api-layer
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/db/migrations/0002_add_consumer_id.sql
  - packages/db/src/types.ts
  - packages/db/src/dal.ts
  - packages/fetch/src/safe-requester.ts
  - packages/fetch/src/index.ts
  - packages/fetch/src/__tests__/safe-requester.test.ts
  - packages/api/package.json
  - packages/api/tsup.config.ts
  - packages/api/vitest.config.ts
  - packages/api/.env.example
  - packages/api/src/index.ts
  - packages/api/src/app.ts
  - packages/api/src/main.ts
  - packages/api/src/__tests__/pglite-helper.ts
  - packages/api/src/__tests__/scaffold.test.ts
autonomous: true
requirements: [API-03, API-05, API-08]
must_haves:
  truths:
    - "audits.consumer_id column exists; insertJob persists it, getJob returns it, listJobs filters by it"
    - "@geo/db tests stay green after the migration + DAL extension"
    - "@geo/fetch exports validateUrlHost (no network) and createSafeRequester (SSRF-safe POST), both reusing the existing resolve-then-pin path"
    - "@geo/fetch tests stay green; a private/loopback callback host is rejected, a public host is allowed"
    - "packages/api builds (tsup) and a trivial OpenAPIHono route test passes under vitest using zod v3"
  artifacts:
    - path: "packages/db/migrations/0002_add_consumer_id.sql"
      provides: "nullable consumer_id column + history index"
      contains: "ADD COLUMN IF NOT EXISTS consumer_id"
    - path: "packages/fetch/src/safe-requester.ts"
      provides: "validateUrlHost + createSafeRequester (SSRF-safe POST)"
      exports: ["validateUrlHost", "createSafeRequester"]
    - path: "packages/api/src/app.ts"
      provides: "createApp({dal, fetcher}) factory returning OpenAPIHono"
      exports: ["createApp"]
  key_links:
    - from: "packages/db/src/dal.ts"
      to: "audits.consumer_id"
      via: "insertJob INSERT + listJobs WHERE + getJob SELECT *"
      pattern: "consumer_id"
    - from: "packages/fetch/src/safe-requester.ts"
      to: "packages/fetch/src/dns-resolve.ts resolveAndValidate"
      via: "reuse internal validator (no duplication)"
      pattern: "resolveAndValidate"
---

<objective>
Wave 0 — prerequisites for the API layer. Three additive, cross-package tasks that unblock Waves 1–2 without changing any existing behavior:
1. `@geo/db`: migration `0002_add_consumer_id.sql` + DAL consumer scoping (D-11).
2. `@geo/fetch`: `validateUrlHost()` (submit-time, no network) + `createSafeRequester()` (SSRF-safe POST for webhook fire), reusing the existing resolve-then-pin validator (D-12).
3. `packages/api`: scaffold — `@geo/api` Bun+Hono package with `createApp({dal,fetcher})` factory, `src/main.ts` Bun.serve entry, PGlite test helper, zod ^3.25 + a zod-v3-compatible `@hono/zod-openapi` (D-14).

Purpose: every downstream slice (submit/poll/history/docs/webhook) depends on these three seams existing.
Output: a green `@geo/db` suite, a green `@geo/fetch` suite, and a building `@geo/api` package with one passing scaffold test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bun-hono-api-layer/05-CONTEXT.md
@.planning/phases/05-bun-hono-api-layer/05-PATTERNS.md

<interfaces>
From packages/db/src/types.ts (extend these — do NOT rewrite):
- InsertJobInput { url; normalizedUrl; urlHash; callbackUrl? }  → ADD consumerId?: string
- PaginationInput { limit; offset }                              → ADD consumerId?: string
- AuditJob { id; url; ...; callbackUrl; ... }                    → ADD consumerId: string | null

From packages/db/src/dal.ts:
- export interface AuditDal { insertJob; getJob; listJobs; findRecentByUrlHash(urlHash, ttlMs); ... }
- createAuditDal(executor: SqlExecutor): AuditDal
- makePgExecutor(sql), getDefaultDal()
- current insertJob INSERT columns: (url, normalized_url, url_hash, callback_url)
- current listJobs SQL: SELECT * FROM audits ORDER BY created_at DESC LIMIT $1 OFFSET $2

From packages/fetch/src/index.ts (current barrel):
- FetchErrorCode, buildErrorResult, isBlockedIP, createSafeFetcher, SafeFetcherOptions
- INTERNAL (not exported): resolveAndValidate, DnsValidationError in ./dns-resolve.js; validateUrl in ./safe-fetcher.js
- createSafeFetcher is GET-only (Fetcher = (url) => Promise<FetchResult>)

From packages/core/src/index.ts: export { normalizeUrl } from "./url.js"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: @geo/db — 0002_add_consumer_id migration + DAL consumer scoping (D-11)</name>
  <read_first>
    - packages/db/migrations/0001_create_audits.sql (header style, IF NOT EXISTS guards, CREATE INDEX IF NOT EXISTS)
    - packages/db/src/types.ts (InsertJobInput/PaginationInput/AuditJob)
    - packages/db/src/dal.ts lines 117-173 (AuditDal interface, insertJob), 307-327 (getJob, listJobs)
    - packages/db/src/__tests__/queue.test.ts (listJobs + findRecentByUrlHash test patterns)
    - 05-PATTERNS.md sections "0002_add_consumer_id.sql", "dal.ts extensions", "types.ts extensions"
  </read_first>
  <behavior>
    - Migration applies on a fresh PGlite DB and is idempotent (re-run is a no-op).
    - After migration, insertJob({...,consumerId:'ottolax'}) persists consumer_id; getJob returns consumerId; AuditJob.consumerId is populated.
    - listJobs({limit,offset,consumerId:'ottolax'}) returns ONLY ottolax rows; listJobs({limit,offset}) (no consumerId) returns all rows (back-compat).
    - Existing Phase 3 rows (null consumer_id) are unaffected; existing db tests stay green.
  </behavior>
  <action>
    Create `packages/db/migrations/0002_add_consumer_id.sql` mirroring 0001's header-comment + guard style: `ALTER TABLE audits ADD COLUMN IF NOT EXISTS consumer_id text;` then `CREATE INDEX IF NOT EXISTS idx_audits_consumer_id ON audits (consumer_id, created_at DESC) WHERE consumer_id IS NOT NULL;`. Header comment names migration 0002, cites D-11, states additive/nullable/non-breaking.
    Extend `packages/db/src/types.ts`: add optional `consumerId?: string` to `InsertJobInput` and `PaginationInput`; add `consumerId: string | null` to `AuditJob` (per D-11). Map the snake_case `consumer_id` row column to camelCase `consumerId` wherever AuditRow→AuditJob mapping happens (mirror existing column mapping in dal.ts).
    Extend `packages/db/src/dal.ts`: insertJob INSERT adds `consumer_id` as the 5th column/param (`input.consumerId ?? null`); listJobs SQL becomes `SELECT * FROM audits WHERE ($3::text IS NULL OR consumer_id = $3) ORDER BY created_at DESC LIMIT $1 OFFSET $2` with params `[limit, offset, pagination.consumerId ?? null]`; getJob already does `SELECT *` so it picks up the column — ensure the row-mapper surfaces `consumerId`. Do NOT change findRecentByUrlHash (dedup stays cross-consumer per D-04/A3). Use only parameterised `$N` placeholders (no string concat).
    Add a db test (extend queue.test.ts or a new consumer-scope test) covering the three behaviors above.
    Do NOT place fenced code in this action; mirror exact SQL/identifiers from read_first.
  </action>
  <verify>
    <automated>bun run --cwd packages/db test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `bun run --cwd packages/db test -- --run` green (existing + new consumer-scope tests).
    - listJobs with consumerId returns only that consumer's rows; without consumerId returns all.
    - getJob exposes consumerId; insertJob persists it.
  </acceptance_criteria>
  <done>Migration 0002 + DAL/types extended; consumer scoping proven; db suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: @geo/fetch — validateUrlHost + createSafeRequester SSRF-safe POST (D-12)</name>
  <read_first>
    - packages/fetch/src/safe-fetcher.ts (full file — validateUrl internal, resolveAndValidate import, undici request with pinned IP + servername, never-throw FetchResult shape, SafeFetcherOptions.resolver injection seam ~line 47)
    - packages/fetch/src/dns-resolve.ts (resolveAndValidate ~line 77-109; DnsValidationError)
    - packages/fetch/src/errors.ts (FetchErrorCode, buildErrorResult)
    - packages/fetch/src/index.ts (current barrel)
    - packages/fetch/src/__tests__/* (mock-resolver + loopback test patterns from Phase 2)
    - 05-PATTERNS.md section "safe-requester.ts" + "SSRF Resolve-Then-Pin"
  </read_first>
  <behavior>
    - validateUrlHost('https://example.com') with a mock resolver returning a public IP → { ok: true }.
    - validateUrlHost('http://127.0.0.1/x') / RFC1918 host (mock resolver → 127.0.0.1 or 10.x) → { ok: false, code: <FetchErrorCode> } and makes NO network request.
    - validateUrlHost of a non-http(s) scheme or unparseable URL → { ok: false }.
    - createSafeRequester(opts)(url, { method:'POST', body }) re-resolves+validates on the call (TOCTOU-safe), POSTs to the pinned IP, and never throws — returns a FetchResult-style shape with an error field on block/failure.
    - A POST whose host resolves to a blocked IP returns an error result, never connects.
  </behavior>
  <action>
    Create `packages/fetch/src/safe-requester.ts` mirroring safe-fetcher.ts structure. Reuse — do NOT duplicate — the internal `resolveAndValidate` (from ./dns-resolve.js) and the `validateUrl`/IP-classification path; reuse the same `resolver` injection seam (SafeFetcherOptions-style) so tests can inject a mock resolver. Export `validateUrlHost(url: string, opts?)` that resolves all A+AAAA records via resolveAndValidate and returns `{ ok: boolean; code?: FetchErrorCode }` WITHOUT issuing any HTTP request (used at submit time, D-08). Export `createSafeRequester(opts?: SafeRequesterOptions)` returning a callable `(url, { method, body, headers? }) => Promise<FetchResult>` that re-validates the resolved IP then issues a pinned-IP undici request with `servername` set for TLS (mirror safe-fetcher lines ~221-229) and a bounded timeout; never throw — return buildErrorResult on any block/timeout/failure.
    Add to `packages/fetch/src/index.ts` barrel: `export { createSafeRequester, validateUrlHost } from "./safe-requester.js";` and `export type { SafeRequesterOptions } from "./safe-requester.js";`.
    Add `packages/fetch/src/__tests__/safe-requester.test.ts` using the Phase 2 mock-resolver/loopback pattern to assert the behaviors above. Reuse the existing test helpers; do not start a new resolver implementation.
  </action>
  <verify>
    <automated>bun run --cwd packages/fetch test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `bun run --cwd packages/fetch test -- --run` green (existing + new safe-requester tests).
    - validateUrlHost blocks loopback/RFC1918 with no network; allows public host.
    - createSafeRequester POST blocks a private-resolving host and never throws.
    - Barrel exports validateUrlHost, createSafeRequester, SafeRequesterOptions.
  </acceptance_criteria>
  <done>SSRF-safe POST + submit-time host validator added to @geo/fetch, reusing the existing validator; fetch suite green.</done>
</task>

<task type="auto">
  <name>Task 3: packages/api scaffold — @geo/api, createApp factory, main.ts, PGlite helper (D-01/D-14)</name>
  <read_first>
    - packages/db/package.json (exports shape, scripts, devDep versions: tsup 8.5.1, vitest 4.1.8, typescript 6.0.3, @electric-sql/pglite 0.5.1, @types/node 25.9.1)
    - packages/db/tsup.config.ts, packages/db/vitest.config.ts (copy verbatim, add main.ts entry)
    - packages/worker/package.json (bin field shape, workspace dep declarations)
    - _templates/bun-hono-app/src/index.ts (OpenAPIHono, app.doc31, Scalar mount), _templates/bun-hono-app/package.json (hono/@hono/zod-openapi/@scalar versions)
    - packages/db/src/__tests__/harness.ts (makePgliteDb), packages/db/src/__tests__/pglite-executor.ts (makePgliteExecutor), packages/db/src/__tests__/schema.test.ts lines 15-18 (cross-package migration path)
    - packages/db/src/client.ts lines 21-48 (getSql fail-fast guard to mirror for GEO_API_KEYS later)
    - 05-PATTERNS.md sections "package.json", "tsup.config.ts", "vitest.config.ts", "main.ts", "app.ts", "pglite-helper.ts"
  </read_first>
  <action>
    BEFORE adding deps, resolve the zod-v3-compatible versions with `npm view`: run `npm view @hono/zod-openapi versions --json`, `npm view @hono/zod-openapi@<candidate> peerDependencies`, and `npm view @scalar/hono-api-reference@<matching> peerDependencies` to pick the highest `@hono/zod-openapi` whose `zod` peer is v3 (the 0.x line, ~0.19.x) plus a matching `@scalar/hono-api-reference`. Pin `zod@^3.25.51` to match the workspace (D-14) — do NOT pull zod v4. Record the chosen exact versions in the SUMMARY.
    Create `packages/api/package.json` (`@geo/api`) mirroring `packages/db/package.json` exports/scripts (`build`, `test` → `vitest`), add `"bin": { "geo-api": "./dist/main.js" }` (mirror worker), dependencies `hono`, the chosen `@hono/zod-openapi` + `@scalar/hono-api-reference`, `zod@^3.25.51`, and workspace `@geo/db`/`@geo/fetch`/`@geo/core`; devDeps mirror db (tsup, vitest, typescript, @electric-sql/pglite, @types/node). `tsup.config.ts` mirrors db with `entry: ["src/index.ts", "src/main.ts"]`. `vitest.config.ts` copied verbatim (no watch).
    Create `src/app.ts`: `export interface AppDeps { dal: AuditDal; fetcher: ReturnType<typeof createSafeFetcher>; }` and `export function createApp(deps: AppDeps): OpenAPIHono` building a `new OpenAPIHono()`, registering `app.doc31('/openapi.json', { openapi:'3.1.0', info:{ title:'@geo/api', version:'0.1.0' } })` and `app.get('/docs', Scalar({ url:'/openapi.json' }))`. Wave 1/2 add routes/middleware; this task only stands up the factory + docs mounts + a placeholder so the OpenAPI registry is non-empty (mount healthz here only if trivial — otherwise leave for Wave 2; a single registered route is enough for the scaffold test). `src/index.ts` re-exports `createApp`/`AppDeps`.
    Create `src/main.ts` (Bun.serve entry): import `getDefaultDal` from `@geo/db` and `createSafeFetcher` from `@geo/fetch`, call `createApp`, `export default { port: Number(process.env.PORT ?? 8080), fetch: app.fetch }`. Do NOT call getDefaultDal at module top-level in a way that throws on import without DATABASE_URL — defer to invocation (mirror db lazy pattern). Add `packages/api/.env.example` documenting `GEO_API_KEYS=token1:consumer1,token2:consumer2`, `DATABASE_URL`, `PORT` (no real secrets).
    Create `src/__tests__/pglite-helper.ts`: copy/re-export `makePgliteDb` from db harness + `makePgliteExecutor`, run the `packages/db/migrations` (path `join(__dirname,'..','..','..','db','migrations')`) on the PGlite db, expose `makeTestDal()` returning `{ dal: createAuditDal(executor), db }`. Reuse — do not reimplement — db's harness.
    Create `src/__tests__/scaffold.test.ts`: build `createApp({ dal, fetcher })` (PGlite dal + `createSafeFetcher({ resolver: mockResolver })`) and assert `app.request('/openapi.json')` returns 200 with a parseable OpenAPI 3.1 document (has `openapi` + `paths`), and `app.request('/docs')` returns 200.
  </action>
  <verify>
    <automated>bun run --cwd packages/api test -- --run; bun run --cwd packages/api build</automated>
  </verify>
  <acceptance_criteria>
    - `bun run --cwd packages/api test -- --run` green; `bun run --cwd packages/api build` produces dist (index + main).
    - Chosen `@hono/zod-openapi` peer dep is zod v3; workspace zod stays ^3.25 (no v4 pulled).
    - `/openapi.json` returns parseable OpenAPI 3.1; `/docs` returns 200.
    - createApp is a factory taking AppDeps (no module-level singleton DAL).
  </acceptance_criteria>
  <done>@geo/api package builds; createApp factory + main.ts + PGlite helper exist; scaffold test green; zod v3 alignment confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → @geo/db | consumer_id scoping enforced in SQL; parameterised queries only |
| worker/API → external callback host | SSRF: untrusted callback_url resolved + IP-classified before any connect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W0-01 | Tampering | SQL in dal.ts extensions | mitigate | parameterised `$N` placeholders only; no string concat of consumer_id |
| T-05-W0-02 | Elevation | createSafeRequester / validateUrlHost | mitigate | reuse resolveAndValidate (all A+AAAA records); never connect to a blocked IP; never throw |
| T-05-W0-03 | Info disclosure | listJobs consumer scoping | mitigate | WHERE ($3 IS NULL OR consumer_id=$3); Wave 2 always passes a consumerId |
| T-05-W0-SC | Tampering | npm installs (hono/@hono/zod-openapi/@scalar) | accept | packages are from owner's own bootstrap template (_templates/bun-hono-app); legitimacy table in RESEARCH.md; all VERIFIED on npm registry — no [ASSUMED]/[SUS] packages, no blocking checkpoint required |
</threat_model>

<verification>
- `bun run --cwd packages/db test -- --run` green (migration + scoping)
- `bun run --cwd packages/fetch test -- --run` green (SSRF POST + host validator)
- `bun run --cwd packages/api test -- --run` and `build` green (scaffold)
- No DATABASE_URL / GEO_API_KEYS literals committed
</verification>

<success_criteria>
consumer_id scoping live in @geo/db; SSRF-safe POST + submit-time validator live in @geo/fetch; @geo/api scaffold builds with zod v3 and serves /openapi.json + /docs. All three suites green. Unblocks Waves 1–2.
</success_criteria>

<output>
Create `.planning/phases/05-bun-hono-api-layer/05-00-SUMMARY.md` when done. Record the chosen exact `@hono/zod-openapi` + `@scalar/hono-api-reference` versions and the zod peer they resolve to.
</output>
