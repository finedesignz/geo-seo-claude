---
phase: 05-bun-hono-api-layer
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - packages/api/src/middleware/auth.ts
  - packages/api/src/app.ts
  - packages/api/src/routes/audit-post.ts
  - packages/api/src/__tests__/auth.test.ts
  - packages/api/src/__tests__/audit-post.test.ts
autonomous: true
requirements: [API-01, API-04, API-05, API-08]
must_haves:
  truths:
    - "Every protected route returns 401 on missing/invalid bearer; /healthz, /openapi.json, /docs are exempt"
    - "A valid bearer token attaches the resolved consumer_id to the request context"
    - "POST /audit zod-validates {url, callback_url?}, normalizes the url, computes url_hash, dedups, inserts a job, returns {job_id} without blocking"
    - "A repeat URL within TTL returns the cached job_id; a prior failed job does NOT dedup (it re-enqueues)"
    - "A callback_url resolving to a private/loopback IP returns 400 at submit"
  artifacts:
    - path: "packages/api/src/middleware/auth.ts"
      provides: "bearerAuth middleware + parseApiKeys fail-fast"
      exports: ["bearerAuth", "parseApiKeys"]
    - path: "packages/api/src/routes/audit-post.ts"
      provides: "POST /audit OpenAPIHono route"
      contains: "createRoute"
  key_links:
    - from: "packages/api/src/routes/audit-post.ts"
      to: "@geo/db findRecentByUrlHash + insertJob"
      via: "dedup-then-insert with status!=='failed' filter"
      pattern: "findRecentByUrlHash"
    - from: "packages/api/src/routes/audit-post.ts"
      to: "@geo/fetch validateUrlHost"
      via: "submit-time callback_url SSRF check → 400"
      pattern: "validateUrlHost"
---

<objective>
Wave 1 — the submit slice. Add bearer auth across the app and the `POST /audit` endpoint (validate → normalize → dedup → SSRF-check callback → insert → `{job_id}`).

Purpose: after this wave a consumer can authenticate and submit an audit; everything else (poll/history/docs/webhook) builds on the auth context and the job created here.
Output: `auth.ts` middleware, `audit-post.ts` route wired into `createApp`, and tests proving 401 semantics, happy-path submit, dedup (incl. failed-job re-enqueue), and callback SSRF rejection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bun-hono-api-layer/05-CONTEXT.md
@.planning/phases/05-bun-hono-api-layer/05-PATTERNS.md

<interfaces>
From 05-00 (Wave 0):
- createApp(deps: AppDeps): OpenAPIHono   // AppDeps = { dal: AuditDal; fetcher }
- @geo/db: insertJob({url, normalizedUrl, urlHash, callbackUrl?, consumerId?}), findRecentByUrlHash(urlHash, ttlMs): Promise<AuditJob|null>
- AuditJob.status: "queued"|"running"|"done"|"failed"; AuditJob.id
- @geo/fetch: validateUrlHost(url, opts?): Promise<{ ok: boolean; code? }>  (no network)
- packages/api test helper: makeTestDal() → { dal, db }
From @geo/core: normalizeUrl(url) → string

Auth middleware shape (RESEARCH.md Pattern 4):
- EXEMPT = new Set(['/healthz','/openapi.json','/docs'])
- Authorization: Bearer <token> → apiKeys.get(token) → c.set('consumer_id', id)
- 401 + WWW-Authenticate: Bearer realm="geo-api"; use crypto.timingSafeEqual for token compare
- parseApiKeys(raw): CSV "tok:id,tok2:id2"; throw if missing/empty (mirror getSql guard)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bearerAuth middleware + parseApiKeys, wired into createApp (API-05)</name>
  <read_first>
    - packages/db/src/client.ts lines 21-48 (fail-fast env guard to mirror)
    - 05-PATTERNS.md section "middleware/auth.ts" + "Fail-Fast Env Guard"
    - packages/api/src/app.ts (createApp from Wave 0 — add middleware registration)
    - _templates/bun-hono-app middleware/createMiddleware usage
  </read_first>
  <behavior>
    - GET/POST to a protected route with no Authorization header → 401 with body { error:'unauthorized', message } and a WWW-Authenticate header.
    - Bearer token not in GEO_API_KEYS → 401.
    - Valid token → handler runs and c.get('consumer_id') equals the mapped consumer.
    - /healthz, /openapi.json, /docs → reachable WITHOUT a token (exempt).
    - parseApiKeys(undefined) and parseApiKeys('') throw with a GEO_API_KEYS message; parseApiKeys('a:x,b:y') → Map size 2.
  </behavior>
  <action>
    Create `packages/api/src/middleware/auth.ts`: `parseApiKeys(raw: string|undefined): Map<string,string>` mirroring the db `getSql` fail-fast guard (throw a `@geo/api: GEO_API_KEYS is required...` error when missing; CSV-parse `tok:id` pairs; throw if the parsed map is empty). `bearerAuth(apiKeys: Map<string,string>)` returns a Hono `createMiddleware` that: short-circuits `next()` for paths in `EXEMPT = new Set(['/healthz','/openapi.json','/docs'])`; reads `Authorization`, strips the `Bearer ` prefix, looks up the consumer (compare with `crypto.timingSafeEqual` against candidate keys to avoid timing leaks); on miss returns `c.json({error:'unauthorized', message}, 401)` after setting `c.header('WWW-Authenticate','Bearer realm="geo-api"')`; on hit `c.set('consumer_id', consumerId)` then `next()`. Never log the Authorization header.
    Update `createApp` to accept the api-keys map (extend AppDeps with `apiKeys: Map<string,string>`, or parse inside createApp from an injected value — keep it injectable for tests, do NOT read process.env inside createApp) and register `app.use('*', bearerAuth(apiKeys))` before routes. `main.ts` calls `parseApiKeys(process.env.GEO_API_KEYS)` at startup (fail-fast) and passes the map in.
    Create `packages/api/src/__tests__/auth.test.ts` covering all behaviors above via `app.request()` against a createApp built with a known keys map; assert exemptions reach 200 (or the route's status) without a token.
    No fenced code in this action.
  </action>
  <verify>
    <automated>bun run --cwd packages/api test -- --run auth</automated>
  </verify>
  <acceptance_criteria>
    - 401 (with WWW-Authenticate) on missing AND invalid token for a protected route.
    - Valid token attaches consumer_id; exempt paths reachable tokenless.
    - parseApiKeys fail-fast on empty/missing.
  </acceptance_criteria>
  <done>bearerAuth + parseApiKeys implemented, wired into createApp, auth tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /audit — validate, normalize, dedup, callback SSRF, insert (API-01/04/08)</name>
  <read_first>
    - _templates/bun-hono-app/src/routes/example.ts (createRoute + app.openapi pattern, c.req.valid, error schema)
    - packages/api/src/middleware/auth.ts (Task 1 — consumer_id on context)
    - @geo/db dal.ts insertJob + findRecentByUrlHash signatures (from interfaces)
    - @geo/core normalizeUrl
    - @geo/fetch validateUrlHost (Wave 0)
    - 05-PATTERNS.md section "audit-post.ts" (dedup status filter, SSRF submit check, error envelope)
    - 05-CONTEXT.md D-04 + D-13 (dedup semantics: done OR queued|running = hit; failed = miss)
  </read_first>
  <behavior>
    - POST /audit {url:'https://example.com'} with valid token → 200 { job_id } and a row exists in audits with consumer_id set; returns fast (no await on any audit work).
    - Invalid/non-http(s) url or malformed body → 400 (zod) with { error, message }.
    - Second POST of the same url within DEDUP_TTL → returns the SAME job_id, no new row (dedup hit on done/queued/running).
    - If the recent matching job has status 'failed' → a NEW job is inserted (no dedup), a different job_id returned.
    - POST with callback_url resolving to loopback/RFC1918 (mock resolver) → 400 { error:'ssrf_blocked', message }, no job inserted.
    - A valid public callback_url → accepted, persisted on the job.
  </behavior>
  <action>
    Create `packages/api/src/routes/audit-post.ts` as an OpenAPIHono route via `createRoute` (method 'post', path '/audit', tags ['audit'], security [{BearerAuth:[]}], request body zod `z.object({ url: z.string().url(), callback_url: z.string().url().optional() })`, responses 200 `{job_id:z.string()}`, 400 + 401 error envelopes `z.object({error:z.string(),message:z.string()})`). Handler: read validated body; `const normalized = normalizeUrl(url)`; compute `url_hash` with `crypto.createHash('sha256').update(normalized).digest('hex')` (normalized url is the hash basis); `const recent = await deps.dal.findRecentByUrlHash(urlHash, DEDUP_TTL_MS)`; if `recent && recent.status !== 'failed'` return `c.json({ job_id: recent.id }, 200)` (D-13). Else, if `callback_url` present, `const ssrf = await deps.fetcher`-style host check via `validateUrlHost(callback_url, { resolver })` — inject the resolver through AppDeps so tests can mock it — and on `!ssrf.ok` return `c.json({ error:'ssrf_blocked', message:'callback_url resolves to a blocked host' }, 400)`. Then `const job = await deps.dal.insertJob({ url, normalizedUrl: normalized, urlHash, callbackUrl: callback_url, consumerId: c.get('consumer_id') })` and return `c.json({ job_id: job.id }, 200)`. Define `DEDUP_TTL_MS` as a module const (document the value; reuse a sane default e.g. matching CONTEXT's TTL intent). Do NOT block on audit execution — the worker handles it.
    Register the route in `createApp` via `app.openapi(...)` (never `app.post`). Ensure `validateUrlHost`'s resolver is reachable from AppDeps (extend AppDeps if needed; default to the real resolver in main.ts, mock in tests).
    Create `packages/api/src/__tests__/audit-post.test.ts` (PGlite-backed via makeTestDal) covering all six behaviors, including the failed-job-re-enqueue case (seed a failed job with the same url_hash then POST and assert a new job_id) and the loopback-callback 400 using the Phase 2 mock resolver.
  </action>
  <verify>
    <automated>bun run --cwd packages/api test -- --run audit</automated>
  </verify>
  <acceptance_criteria>
    - Happy path → 200 {job_id}, row persisted with consumer_id.
    - Invalid url → 400; dedup hit → same job_id, no new row; failed prior → new job_id.
    - Loopback/RFC1918 callback_url → 400 ssrf_blocked, no insert; public callback accepted.
    - Route registered via app.openapi (appears in /openapi.json paths).
  </acceptance_criteria>
  <done>POST /audit implemented with auth, validation, dedup (failed-skip), submit-time SSRF, async insert; audit-post tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | bearer token authenticates; untrusted url + callback_url cross here |
| API → callback host | callback_url is SSRF-validated before acceptance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Spoofing/Elevation | every protected route | mitigate | bearerAuth → 401 on missing/invalid; exemptions limited to /healthz,/openapi.json,/docs |
| T-05-01-02 | Info disclosure | token comparison | mitigate | crypto.timingSafeEqual; never log Authorization header |
| T-05-01-03 | Elevation/SSRF | POST /audit callback_url | mitigate | validateUrlHost at submit → 400 on private/loopback (re-validated at fire in Wave 2) |
| T-05-01-04 | Tampering | POST /audit body | mitigate | zod validation via createRoute → automatic 400 |
| T-05-01-05 | Info disclosure | dedup cross-consumer | accept | findRecentByUrlHash is intentionally cross-consumer (same URL = same result; internal 2-consumer service, A3) — documented, not a leak of consumer-private data |
</threat_model>

<verification>
- `bun run --cwd packages/api test -- --run` green (auth + audit-post)
- 401 proven on every protected route; submit happy-path + dedup + failed-re-enqueue + callback SSRF proven
- No token / API key literals committed
</verification>

<success_criteria>
ROADMAP criteria 1 (POST returns {job_id}; same URL within TTL returns cached) and 4 (unauth → 401) hold; callback SSRF rejection (criterion 7, submit half) holds. Auth + submit slice green.
</success_criteria>

<output>
Create `.planning/phases/05-bun-hono-api-layer/05-01-SUMMARY.md` when done.
</output>
