---
phase: 05-bun-hono-api-layer
plan: 02
type: execute
wave: 2
depends_on: ["05-00", "05-01"]
files_modified:
  - packages/api/src/routes/audit-get.ts
  - packages/api/src/routes/audits-list.ts
  - packages/api/src/routes/healthz.ts
  - packages/api/src/app.ts
  - packages/api/docs/api.md
  - packages/api/src/__tests__/audit-get.test.ts
  - packages/api/src/__tests__/audits-list.test.ts
  - packages/api/src/__tests__/healthz.test.ts
  - packages/api/src/__tests__/openapi.test.ts
  - packages/worker/src/webhook.ts
  - packages/worker/src/pipeline.ts
  - packages/worker/src/__tests__/webhook.test.ts
autonomous: true
requirements: [API-02, API-03, API-06, API-07, API-08]
must_haves:
  truths:
    - "GET /audit/{id} returns status; when done returns score+findings; not-found OR not-owned returns 404 (no cross-consumer read)"
    - "GET /audits returns a paginated list scoped to the authenticated consumer"
    - "GET /healthz returns 200 {db:'ok'} when Postgres reachable, 503 {db:'error'} when not"
    - "GET /openapi.json is a valid OpenAPI 3.x doc with a BearerAuth security scheme; GET /docs renders Scalar; docs/api.md is generated + committed"
    - "On job completion (and terminal failure) the worker fires the webhook via the SSRF-safe POST, re-validating the host at fire time; failures are non-fatal"
  artifacts:
    - path: "packages/api/src/routes/audit-get.ts"
      provides: "GET /audit/{job_id} with consumer ownership check"
      contains: "createRoute"
    - path: "packages/api/src/routes/healthz.ts"
      provides: "deep DB health route 200/503"
      contains: "SELECT 1"
    - path: "packages/worker/src/webhook.ts"
      provides: "deliverWebhook via createSafeRequester"
      exports: ["deliverWebhook"]
    - path: "packages/api/docs/api.md"
      provides: "generated API docs (rule 21)"
  key_links:
    - from: "packages/api/src/routes/audit-get.ts"
      to: "consumer_id ownership"
      via: "job.consumerId !== c.get('consumer_id') → 404"
      pattern: "consumerId"
    - from: "packages/worker/src/pipeline.ts"
      to: "packages/worker/src/webhook.ts deliverWebhook"
      via: "non-fatal fire after completeJob / terminal failJob"
      pattern: "deliverWebhook"
---

<objective>
Wave 2 — the read + docs + webhook slice. Add `GET /audit/{id}` (poll, ownership-scoped), `GET /audits` (paginated, consumer-scoped), `GET /healthz` (deep DB check), `/openapi.json` security scheme + `/docs` Scalar + generated `docs/api.md`, and the webhook delivery module wired into the worker (fire-time SSRF re-validation, non-fatal).

Purpose: completes the public API surface and closes the webhook half of API-08 — the full async loop (submit → poll/history → callback) is now callable and documented.
Output: three read routes, the OpenAPI/docs surface, and `webhook.ts` fired from `pipeline.ts`. All suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bun-hono-api-layer/05-CONTEXT.md
@.planning/phases/05-bun-hono-api-layer/05-PATTERNS.md

<interfaces>
From Wave 0/1:
- createApp(deps): OpenAPIHono (auth registered); app.doc31('/openapi.json',...); /docs Scalar mounted
- @geo/db: getJob(id): Promise<AuditJob|null> (AuditJob has consumerId, status, score, findings); listJobs({limit, offset, consumerId?}); getSql() / DAL select for SELECT 1
- AuditJob.status: "queued"|"running"|"done"|"failed"
- @geo/fetch: createSafeRequester(opts?)(url,{method,body}) → FetchResult (re-validates host, never throws)
- packages/api test helper: makeTestDal() → { dal, db }

From packages/worker/src/pipeline.ts (extend — read for exact call sites):
- completeJob success path (~line 158); three failJob call sites (~lines 82,134,143)
- job.callbackUrl available on the job object
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /audit/{id} (ownership 404) + GET /audits (paginated, scoped) + GET /healthz (API-02/03/06)</name>
  <read_first>
    - _templates/bun-hono-app/src/routes/example.ts (createRoute params + app.openapi) and src/index.ts healthRoute
    - packages/api/src/middleware/auth.ts (consumer_id on context)
    - @geo/db getJob / listJobs / getSql (interfaces) ; dal.ts listJobs consumer filter from Wave 0
    - 05-PATTERNS.md sections "audit-get.ts" (ownership check), "audits-list.ts" (page/limit→offset), "healthz.ts" (deep SELECT 1, 200/503)
  </read_first>
  <behavior>
    - GET /audit/{id} owned + done → 200 { status:'done', score, findings }.
    - GET /audit/{id} owned + queued/running → 200 { status } (no score/findings).
    - GET /audit/{id} not found → 404; owned by a DIFFERENT consumer → 404 (no cross-consumer read).
    - GET /audits → 200 { jobs, page, limit }, returns ONLY the authenticated consumer's jobs; page/limit query coerced + capped (limit ≤ 100, defaults page=1 limit=20).
    - GET /healthz with reachable DB → 200 { status:'ok', db:'ok' }; with DB down/closed → 503 { status:'error', db:'error' }; reachable WITHOUT a token (exempt).
  </behavior>
  <action>
    Create `packages/api/src/routes/audit-get.ts` (createRoute GET '/audit/{job_id}', params zod, security BearerAuth, responses 200/404). Handler: `const job = await deps.dal.getJob(id)`; `if (!job || job.consumerId !== c.get('consumer_id')) return c.json({error:'not_found', message:'Job not found'}, 404)`; body is `job.status==='done' ? { status, score: job.score, findings: job.findings } : { status: job.status }`; 200.
    Create `packages/api/src/routes/audits-list.ts` (createRoute GET '/audits', query zod `z.object({ page: z.coerce.number().int().min(1).default(1), limit: z.coerce.number().int().min(1).max(100).default(20) })`). Handler: `const offset=(page-1)*limit`; `const jobs = await deps.dal.listJobs({ consumerId: c.get('consumer_id'), limit, offset })`; return `c.json({ jobs, page, limit }, 200)`.
    Create `packages/api/src/routes/healthz.ts` (createRoute GET '/healthz', no security, responses 200/503 with literal schemas). Handler: try a deep `SELECT 1` (via `deps.dal` select-one or `getSql().unsafe('SELECT 1')` — prefer a dal method so tests can drive a closed PGlite db); on success `c.json({status:'ok',db:'ok'},200)`, on throw `c.json({status:'error',db:'error'},503)`. Confirm '/healthz' is in the auth EXEMPT set (Wave 1).
    Register all three in `createApp` via `app.openapi(...)`.
    Tests (PGlite via makeTestDal): `audit-get.test.ts` (done/in-flight/not-found/cross-consumer-404), `audits-list.test.ts` (pagination + consumer scoping: seed jobs for two consumers, assert only the caller's returned), `healthz.test.ts` (200 reachable; 503 after closing the PGlite db to force the SELECT 1 to throw).
  </action>
  <verify>
    <automated>bun run --cwd packages/api test -- --run poll history healthz</automated>
  </verify>
  <acceptance_criteria>
    - Cross-consumer GET → 404; owned done → 200 with score+findings; in-flight → 200 status-only.
    - /audits paginated + scoped to caller; limit capped at 100.
    - /healthz 200 db:ok reachable, 503 db:error when DB down; tokenless reachable.
  </acceptance_criteria>
  <done>Three read routes implemented + registered; poll/history/healthz tests green.</done>
</task>

<task type="auto">
  <name>Task 2: /openapi.json BearerAuth security scheme + /docs + generated docs/api.md (API-07)</name>
  <read_first>
    - _templates/bun-hono-app/src/index.ts (app.doc31 + Scalar mount)
    - RESEARCH.md Pattern 2 (registerComponent securitySchemes BearerAuth) — confirm exact API for the chosen @hono/zod-openapi version before coding
    - packages/api/src/app.ts (Wave 0 doc31/Scalar; routes registered in Task 1 + Wave 1)
    - global rule 21 backend adapter (emit /openapi.json + docs/api.md, CI mode backend)
  </read_first>
  <action>
    In `createApp` (app.ts), register the bearer security scheme so it appears in the spec: use `app.openAPIRegistry.registerComponent('securitySchemes','BearerAuth',{ type:'http', scheme:'bearer' })` (or the doc31 `components.securitySchemes` option — verify which the chosen @hono/zod-openapi version supports; the protected routes already reference `security:[{BearerAuth:[]}]`). Confirm `/openapi.json` (3.1) lists all paths (/audit POST+GET, /audits, /healthz) and `/docs` returns Scalar 200.
    Generate `packages/api/docs/api.md` from the live `/openapi.json` (rule 21): add a small `scripts/gen-docs` step in package.json (or a `bun run` one-shot) that boots createApp, fetches `/openapi.json`, and writes a markdown rendering to `docs/api.md`; commit the generated file. Document the regeneration command in the SUMMARY.
    Create `packages/api/src/__tests__/openapi.test.ts`: assert `/openapi.json` parses as OpenAPI 3.x, contains `components.securitySchemes.BearerAuth`, lists the four route paths, and `/docs` returns 200.
    No fenced code in this action.
  </action>
  <verify>
    <automated>bun run --cwd packages/api test -- --run openapi</automated>
  </verify>
  <acceptance_criteria>
    - /openapi.json valid OpenAPI 3.x with BearerAuth security scheme + all four paths.
    - /docs returns 200 (Scalar).
    - docs/api.md generated from the spec and committed.
  </acceptance_criteria>
  <done>OpenAPI security scheme registered, Scalar docs live, docs/api.md generated; openapi test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: webhook delivery module + wire worker pipeline (API-08 fire half)</name>
  <read_first>
    - packages/worker/src/pipeline.ts (completeJob success path ~line 158; failJob sites ~82,134,143; job.callbackUrl; PipelineDeps deps pattern lines 28-46)
    - packages/worker/src/__tests__/pipeline.test.ts (existing test harness/mocks)
    - @geo/fetch createSafeRequester (Wave 0) — SSRF-safe POST, re-validates host, never throws
    - 05-PATTERNS.md sections "webhook.ts" + "pipeline.ts extensions"
    - 05-CONTEXT.md D-08 (re-validate at fire; bounded timeout + retries; non-fatal)
  </read_first>
  <behavior>
    - deliverWebhook(callbackUrl, payload) POSTs {job_id,status,score,findings} via createSafeRequester (host re-validated at fire).
    - A callbackUrl resolving to a private IP at fire time → delivery is blocked (createSafeRequester returns error); deliverWebhook does NOT throw.
    - Delivery failure (timeout / non-2xx / SSRF block) is logged and swallowed — never rejects, never marks the job failed.
    - On job completion with a callbackUrl, pipeline fires deliverWebhook once (status 'done', score, findings); on terminal failJob with a callbackUrl, fires with status 'failed'.
    - A job with no callbackUrl triggers no delivery.
  </behavior>
  <action>
    Create `packages/worker/src/webhook.ts`: `export async function deliverWebhook(callbackUrl: string, payload: { job_id: string; status: string; score: number|null; findings: unknown }): Promise<void>` that builds `createSafeRequester({ timeoutMs: 5000 })`, loops up to MAX_RETRIES (2) issuing a POST with `JSON.stringify(payload)` + `Content-Type: application/json`; returns on the first non-error result; on error logs `console.warn('[webhook] ...')` and continues; always resolves (non-fatal). Keep the requester injectable (optional deps arg) so the test can inject a mock resolver / requester.
    Extend `packages/worker/src/pipeline.ts`: after the `completeJob` success path (~line 158) add a non-fatal fire — `if (job.callbackUrl && ok) deliverWebhook(job.callbackUrl, { job_id: job.id, status:'done', score: scored.score, findings: merged }).catch(err => console.warn('[pipeline] webhook delivery failed:', err))`. Add the same non-fatal fire (status 'failed') at each of the three terminal `failJob` call sites. DRY them behind a small local `tryDeliverWebhook(job, status, score, findings)` helper. This is additive — do NOT change job state transitions or make webhook delivery block/await the job result.
    Create `packages/worker/src/__tests__/webhook.test.ts`: assert deliverWebhook POSTs on a public host (mock resolver → public IP, mock requester captures body), returns without throwing when the host resolves private (mock resolver → 127.0.0.1), and swallows a failing requester. Add/extend a pipeline test asserting the completion path fires the webhook for a job with callbackUrl and does NOT for a job without one (reuse the existing pipeline test harness; keep delivery non-fatal — a failing webhook must not fail the job).
  </action>
  <verify>
    <automated>bun run --cwd packages/worker test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - deliverWebhook never throws; blocks private-resolving hosts at fire time; swallows failures.
    - Pipeline fires webhook on completion + terminal failure when callbackUrl present; skips when absent; job status unaffected by delivery outcome.
    - `bun run --cwd packages/worker test -- --run` green.
  </acceptance_criteria>
  <done>webhook.ts added + wired into pipeline (4 non-fatal sites); fire-time SSRF re-validation proven; worker suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | bearer token; consumer_id scopes reads |
| worker → callback host | callback_url re-resolved + IP-classified at fire time (TOCTOU) before POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Info disclosure | GET /audit/{id}, GET /audits | mitigate | consumerId ownership check → 404 on others' jobs; listJobs WHERE consumer_id |
| T-05-02-02 | Elevation/SSRF | webhook fire | mitigate | createSafeRequester re-resolves + validates host before POST (TOCTOU); never connects to blocked IP |
| T-05-02-03 | DoS | webhook delivery | mitigate | bounded timeout (5s) + capped retries (2); non-fatal — never blocks/fails the job |
| T-05-02-04 | Info disclosure | /openapi.json | accept | spec exposes route shapes only; no secrets; /healthz/openapi/docs intentionally public |
</threat_model>

<verification>
- `bun run --cwd packages/api test -- --run` green (poll/history/healthz/openapi)
- `bun run --cwd packages/worker test -- --run` green (webhook + pipeline wiring)
- `/openapi.json` validates as OpenAPI 3.x with BearerAuth; `docs/api.md` committed
- cross-consumer GET → 404; /healthz 200 vs 503 proven
</verification>

<success_criteria>
ROADMAP criteria 2 (poll done → score+findings), 3 (paginated consumer history), 5 (/healthz 200/non-200), 6 (/openapi.json + Scalar /docs), and 7 (webhook fires on completion; fire-time SSRF re-validation) all hold. Full API surface callable + documented; webhook loop closed.
</success_criteria>

<output>
Create `.planning/phases/05-bun-hono-api-layer/05-02-SUMMARY.md` when done. Record the docs/api.md regeneration command.
</output>
