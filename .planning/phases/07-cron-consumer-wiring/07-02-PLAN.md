---
phase: 07-cron-consumer-wiring
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - Dockerfile
  - .env.example
  - docs/deploy.md
autonomous: true
requirements: [DEPLOY-02, DEPLOY-03]
user_setup:
  - service: coolify-scheduled-task
    why: "Register the cron run target as a Coolify Scheduled Task (live firing is DEFERRED-LIVE behind the Phase 6 operator deploy gate)"
    env_vars:
      - name: CRON_TARGET_URLS
        source: "Operator-chosen list of site URLs to re-audit (comma/newline)"
      - name: CRON_API_TOKEN
        source: "A GEO_API_KEYS bearer mapped to consumer_id `cron`"
      - name: GEO_API_BASE_URL
        source: "Internal URL of the deployed geo-api service"
    dashboard_config:
      - task: "Add Scheduled Task: command `bun packages/cron/dist/main.js`, frequency `0 4 * * *` (UTC, > 1h dedup window)"
        location: "Coolify -> geo-api resource -> Scheduled Tasks"
      - task: "Append `,<cron-token>:cron` to GEO_API_KEYS env"
        location: "Coolify -> geo-api resource -> Environment Variables"

must_haves:
  truths:
    - "The cron run target builds into the same single image (its package.json is copied in the deps stage)"
    - "Operators have a documented Coolify Scheduled-Task runbook + the > 1h cadence constraint"
    - ".env.example documents CRON_TARGET_URLS, CRON_API_TOKEN, GEO_API_BASE_URL, CRON_SCHEDULE and the :cron/:ottolax GEO_API_KEYS entries"
  artifacts:
    - path: "Dockerfile"
      provides: "cron package manifest copied so its dist/main.js ships in the image"
      contains: "packages/cron/package.json"
    - path: "docs/deploy.md"
      provides: "fourth run target (Cron) + scheduled-task runbook + cadence warning"
      contains: "Scheduled Task"
    - path: ".env.example"
      provides: "cron env documentation"
      contains: "CRON_TARGET_URLS"
  key_links:
    - from: "Dockerfile deps stage"
      to: "packages/cron/dist/main.js"
      via: "manifest copy + existing `bun run --filter '*' build`"
      pattern: "packages/cron/package.json"
---

<objective>
Make the cron a real run target off the existing single image and document the operator
runbook. Add the cron manifest copy to the Dockerfile (so `packages/cron/dist/main.js`
ships), expand `.env.example` with the cron + consumer env vars, and append a Cron /
scheduled-re-audit section to `docs/deploy.md` (Coolify Scheduled Task, default `0 4 * * *`
UTC, the mandatory > 1h cadence vs dedup constraint, the `:cron` GEO_API_KEYS entry).

Purpose: DEPLOY-02 deployability + DEPLOY-03 env-sourced secrets. All edits are additive/docs.
Live scheduled firing is DEFERRED-LIVE (Phase 6 operator gate) — captured as user_setup.

Output: edited Dockerfile, .env.example, docs/deploy.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-cron-consumer-wiring/07-CONTEXT.md
@.planning/phases/07-cron-consumer-wiring/07-RESEARCH.md
@Dockerfile
@docs/deploy.md
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cron manifest to Dockerfile deps stage</name>
  <files>Dockerfile</files>
  <action>
    In the `deps` stage manifest-copy block (after the `packages/worker/package.json` line, ~L25),
    add `COPY packages/cron/package.json ./packages/cron/package.json`. Update the header comment
    block (L4-9 "Roles (one image...)") to add a fourth role line:
    `Cron (scheduled task) : bun packages/cron/dist/main.js (one-shot; Coolify cron expr)`.
    Do NOT add a new build stage or CMD — the existing `bun run --filter '*' build` already builds the
    new package and the runtime stage already copies `--from=build /app/packages`, so
    `packages/cron/dist/main.js` ships automatically (RESEARCH "Dockerfile change"). Surgical: only the
    one COPY line + the comment update.
  </action>
  <verify>
    <automated>grep -q "packages/cron/package.json" Dockerfile && echo OK</automated>
  </verify>
  <done>Dockerfile copies the cron manifest in deps stage; header documents the fourth run target; no new stage/CMD added.</done>
</task>

<task type="auto">
  <name>Task 2: Document cron env (.env.example) + Coolify scheduled-task runbook (deploy.md)</name>
  <files>.env.example, docs/deploy.md</files>
  <action>
    `.env.example`: append a `# === CRON / SCHEDULED RE-AUDIT (Phase 7) ===` block documenting
    `CRON_TARGET_URLS` (comma/newline list of site URLs — example with two placeholder URLs),
    `CRON_API_TOKEN` (the cron consumer's bearer; placeholder, never a real token),
    `GEO_API_BASE_URL` (internal geo-api URL; default `http://localhost:8080`), and `CRON_SCHEDULE`
    (documented as the value the operator enters into Coolify's task config — default `0 4 * * *`,
    note "UTC; MUST fire less than once per hour — see deploy.md"). Also extend the existing
    `GEO_API_KEYS` example to show the `:cron` and `:ottolax` consumer entries
    (e.g. `...,<cron-token>:cron,<ottolax-token>:ottolax`). All values are placeholders.
    `docs/deploy.md`: (a) add a `| Cron | ...` row to the "one image, three run targets" table
    (start command `bun packages/cron/dist/main.js`, Network none, Health one-shot) and rename the
    "three" to "four" run targets. (b) Append a new `## Cron / scheduled re-audit (DEPLOY-02)` section
    covering: register a Coolify **Scheduled Task** on the geo-api resource with command
    `bun packages/cron/dist/main.js` and frequency `0 4 * * *` (mark `[HUMAN GATE]`); add a `:cron`
    entry to `GEO_API_KEYS` and set `CRON_TARGET_URLS`/`CRON_API_TOKEN`/`GEO_API_BASE_URL`; a loud
    **cadence warning** (the 1h consumer-scoped `DEDUP_TTL_MS` means `CRON_SCHEDULE` MUST fire less
    often than hourly or re-audits silently dedup — Pitfall 1); a UTC/timezone note (Pitfall 2); a
    note that the one-shot only ENQUEUES (the worker does the slow work async) so overlap is unlikely
    (Pitfall 4); and an explicit **DEFERRED-LIVE** callout that live firing + jobs-in-history are
    verified after the operator deploy (mirrors Phase 6). Keep `/openapi.json` as the contract source
    of truth; do not re-spec the HTTP schema here.
  </action>
  <verify>
    <automated>grep -q "CRON_TARGET_URLS" .env.example && grep -qi "Scheduled Task" docs/deploy.md && grep -qi "less than once per hour\|> 1h\|less often than hourly" docs/deploy.md && echo OK</automated>
  </verify>
  <done>.env.example documents all CRON_* vars + :cron/:ottolax keys; deploy.md has the cron run-target row, the scheduled-task runbook, the > 1h cadence warning, UTC note, and a DEFERRED-LIVE callout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (Coolify env) -> cron/api | secrets (CRON_API_TOKEN, GEO_API_KEYS) injected at runtime, never baked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | .env.example / docs | mitigate | Only placeholder tokens documented; `.dockerignore` already drops `.env*`; docs instruct secrets live in Coolify env (DEPLOY-03), never committed. |
| T-07-06 | Tampering | sub-1h CRON_SCHEDULE | mitigate (doc) | deploy.md loudly documents the > 1h cadence constraint so re-audits don't silently dedup (Pitfall 1). |
</threat_model>

<verification>
- `grep packages/cron/package.json Dockerfile` matches; no new build stage/CMD.
- `.env.example` documents CRON_TARGET_URLS / CRON_API_TOKEN / GEO_API_BASE_URL / CRON_SCHEDULE + :cron/:ottolax keys, all placeholders.
- `docs/deploy.md` has the cron run-target row, scheduled-task runbook, > 1h cadence warning, UTC note, DEFERRED-LIVE callout.
- No real secret committed anywhere.
</verification>

<success_criteria>
- Cron ships in the single image (manifest copied; dist built + copied by existing stages).
- Operators have a complete Coolify Scheduled-Task runbook + cadence/timezone guidance.
- Env contract documented for cron + both consumers.
- (DEFERRED-LIVE) actual Coolify scheduled task registration + live firing — operator action, captured in user_setup.
</success_criteria>

<output>
Create `.planning/phases/07-cron-consumer-wiring/07-02-SUMMARY.md` when done.
</output>
