---
phase: 07-cron-consumer-wiring
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - examples/how-inline-usage.ts
  - examples/how-inline-usage.test.ts
  - examples/ottolax-client.py
  - examples/vitest.config.ts
  - docs/consumers.md
autonomous: true
requirements: [CONS-01, CONS-02]
user_setup: []

must_haves:
  truths:
    - "examples/how-inline-usage.ts imports @geo/core and runs checkRobots + detectRendering with an injected Fetcher and NO network"
    - "A vitest test proves the @geo/core inline usage runs offline (no real network), asserting structured results (CONS-01 PROVEN NOW)"
    - "examples/ottolax-client.py is a stdlib-only (urllib) client: POST /audit with Bearer, poll GET /audit/{id} until terminal, print score+findings, matching the verified JSON contract"
    - "docs/consumers.md documents both consumer integration patterns, points at /openapi.json as the contract source of truth, and marks cross-repo wiring as a deferred follow-up"
  artifacts:
    - path: "examples/how-inline-usage.ts"
      provides: "CONS-01 runnable inline import"
      contains: "@geo/core"
    - path: "examples/how-inline-usage.test.ts"
      provides: "CONS-01 offline proof"
    - path: "examples/ottolax-client.py"
      provides: "CONS-02 stdlib Python HTTP client"
      contains: "urllib"
    - path: "docs/consumers.md"
      provides: "consumer integration + contract doc"
      contains: "openapi.json"
  key_links:
    - from: "examples/how-inline-usage.ts"
      to: "@geo/core checkRobots/detectRendering"
      via: "direct package import, injected Fetcher, no HTTP"
      pattern: "checkRobots|detectRendering"
    - from: "examples/ottolax-client.py"
      to: "POST /audit + GET /audit/{id}"
      via: "urllib.request with Authorization: Bearer"
      pattern: "/audit"
---

<objective>
Deliver the two consumer-facing integration artifacts + the contract doc:
1. CONS-01: a runnable `@geo/core` inline-import example (`examples/how-inline-usage.ts`) +
   a vitest test proving it runs OFFLINE (no network) — this fully PROVES success criterion 2 NOW.
2. CONS-02: a stdlib-only Python client (`examples/ottolax-client.py`) matching the verified
   HTTP contract (POST /audit Bearer -> poll GET /audit/{id} -> {status,score,findings}).
3. `docs/consumers.md`: both integration patterns + contract, with `/openapi.json` as source of
   truth (rule 21); the cross-repo wiring (HOW file/workspace dep; ottolax repo integration) is
   documented as a DEFERRED cross-repo follow-up (rule 20 — NOT done in this phase's commits).

The Python live round-trip is DEFERRED-LIVE (needs deployed geo-api). The HOW offline import is
PROVEN NOW. No edits to the HOW or ottolax repos (rule 20).

Output: examples/how-inline-usage.ts (+test +vitest config), examples/ottolax-client.py, docs/consumers.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-cron-consumer-wiring/07-CONTEXT.md
@.planning/phases/07-cron-consumer-wiring/07-RESEARCH.md
@packages/core/src/index.ts

<interfaces>
<!-- VERIFIED @geo/core signatures (RESEARCH Pattern 3, codebase read 2026-06-04). Use directly. -->
checkRobots(siteUrl: string, fetcher: Fetcher): Promise<RobotsResult>   // robots.ts
detectRendering(html: string): RenderingResult                          // synchronous, rendering.ts
Fetcher = (url: string) => Promise<FetchResult>
FetchResult = { url; status; headers; body; redirectChain; error? }
RobotsResult = { url; exists; content; aiCrawlerStatus; sitemaps; errors }
  // aiCrawlerStatus keyed by GPTBot | ClaudeBot | PerplexityBot | GoogleBot | BingBot
RenderingResult = { rendering: 'ssr'|'csr'|'hybrid'; confidence; signals; wordCount; scriptCount; errors }
@geo/core has ZERO runtime deps and NO node: imports — the import is pure/network-free.

<!-- VERIFIED HTTP contract (RESEARCH Pattern 4, audit-post.ts/audit-get.ts read 2026-06-04). -->
POST /audit  body {url, callback_url?}  -> 200 {job_id} | 400 {error,message} | 401
GET /audit/{job_id} -> 200 {status: queued|running|done|failed, score?, findings?, error_code?} | 401 | 404
  // score+findings present ONLY when status==done; error_code ONLY when status==failed.
  // findings is an object (z.record(z.string(), z.unknown())). 404 = not found OR not owned.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CONS-01 @geo/core inline example + offline proof test</name>
  <files>examples/how-inline-usage.ts, examples/how-inline-usage.test.ts, examples/vitest.config.ts</files>
  <behavior>
    - inlineGeoChecks(siteUrl, html) calls checkRobots with an INJECTED Fetcher (returns a canned
      robots.txt string) and detectRendering on the passed html — makes ZERO real network calls
    - robots result exposes aiCrawlerStatus.GPTBot (defined) for the injected robots content
    - rendering.rendering is one of 'ssr' | 'csr' | 'hybrid'
    - The test asserts the above WITHOUT any real fetch (the injected Fetcher is the only I/O seam)
  </behavior>
  <action>
    Create `examples/how-inline-usage.ts` per RESEARCH Pattern 3: import `checkRobots, detectRendering,
    type Fetcher, type FetchResult` from `@geo/core`. Define a `fakeFetcher: Fetcher` returning a static
    `FetchResult` ({url, status:200, headers:{}, redirectChain:[], body: a canned robots.txt with a
    GPTBot Disallow rule}). Export `async function inlineGeoChecks(siteUrl, html)` that returns
    `{ robots: await checkRobots(siteUrl, fakeFetcher), rendering: detectRendering(html) }`. Add an
    `if (import.meta.main)` block that runs it on `https://example.com` + a static HTML string and
    prints JSON — runnable via `bun examples/how-inline-usage.ts`. Add a header comment: this is the
    CONS-01 inline (no-HTTP) path HOW consumes; HOW already has page HTML + robots in hand from its own
    crawl, so it injects them.
    Create `examples/how-inline-usage.test.ts` (vitest): import `inlineGeoChecks` from
    `./how-inline-usage.js`, assert `robots.aiCrawlerStatus.GPTBot` is defined and
    `["ssr","csr","hybrid"]` contains `rendering.rendering`. The test must use NO real network (only the
    injected fetcher) — add a comment asserting that invariant.
    Add `examples/vitest.config.ts` (minimal — globals/node env) so `examples/` tests run. Ensure
    `@geo/core` resolves from `examples/` (it's a workspace package; if examples/ needs its own
    package.json with `@geo/core: workspace:*` + vitest devDep to resolve imports + the test runner,
    add a minimal `examples/package.json` — check whether a root-level vitest already picks up
    `examples/` first; prefer the smallest setup that makes `bun run --cwd examples test` work, OR run
    the test from the repo root if the root config covers examples/).
  </action>
  <verify>
    <automated>bun run --cwd examples test -- --run how-inline-usage</automated>
  </verify>
  <done>examples/how-inline-usage.ts runs offline and prints results; the vitest test passes asserting structured robots + rendering results with no real network (CONS-01 PROVEN NOW).</done>
</task>

<task type="auto">
  <name>Task 2: CONS-02 stdlib Python client + docs/consumers.md</name>
  <files>examples/ottolax-client.py, docs/consumers.md</files>
  <action>
    Create `examples/ottolax-client.py` per RESEARCH Pattern 4 — stdlib ONLY (`json, os, sys, time,
    urllib.request, urllib.error`, NO pip deps). Read `GEO_API_BASE_URL` (default
    `http://localhost:8080`, rstrip slash) and the bearer from env (`os.environ["GEO_API_TOKEN"]` — a
    GEO_API_KEYS entry for the ottolax consumer; NEVER hardcode a token). `_req(method, path, body)`
    builds a `urllib.request.Request` with `Authorization: Bearer <token>` + `Content-Type:
    application/json`. `submit(url)` POSTs `/audit` `{url}` -> returns `job_id`. `poll(job_id)` GETs
    `/audit/{job_id}` on an interval with a BOUNDED timeout (raise TimeoutError on deadline), returning
    when status in `("done","failed")`. `__main__`: take a URL arg (default https://example.com),
    submit, poll, print `{score, findings}` on done or the `error_code` + exit 1 on failed. Match the
    verified contract exactly (status enum, score/findings only on done, error_code only on failed).
    Add a header comment marking the live round-trip as DEFERRED-LIVE (needs deployed geo-api) and the
    ottolax-repo integration as a cross-repo follow-up (rule 20).
    Create `docs/consumers.md` (rule 21 consumer doc): sections —
    (1) Overview: two consumers, two patterns (HOW = inline @geo/core no-HTTP; ottolax = HTTP).
    (2) CONS-01 HOW inline: how to depend on `@geo/core` — recommend a `file:` dep
    (`"@geo/core": "file:../geo-seo-claude/packages/core"` after `bun run build`) as the default, list
    workspace/git/npm-publish alternatives (RESEARCH "@geo/core Publishability"); reference
    `examples/how-inline-usage.ts`; mark the actual HOW-repo package.json edit as a DEFERRED CROSS-REPO
    follow-up (rule 20 — not done in geo-seo-claude commits).
    (3) CONS-02 ottolax HTTP: reference `examples/ottolax-client.py`; describe POST/poll flow and the
    `GEO_API_KEYS` `:ottolax` bearer; point at `/openapi.json` as the contract source of truth (do NOT
    re-spec the JSON schema beyond a brief shape summary); mark the ottolax-repo wiring + the live
    round-trip as DEFERRED (cross-repo + DEFERRED-LIVE respectively).
    (4) A note that `/openapi.json` + `/docs` are authoritative for the HTTP contract.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('examples/ottolax-client.py').read()); print('OK')" && grep -q "urllib" examples/ottolax-client.py && grep -qi "openapi.json" docs/consumers.md && echo OK</automated>
  </verify>
  <done>examples/ottolax-client.py is valid stdlib-only Python matching the verified contract, reads token from env (no hardcoded secret); docs/consumers.md documents both patterns, references /openapi.json, and flags cross-repo + DEFERRED-LIVE follow-ups.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ottolax client -> geo-api HTTP | Python client sends a Bearer token over the network to geo-api |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information Disclosure | ottolax-client.py token handling | mitigate | Token read from `os.environ["GEO_API_TOKEN"]` ONLY — never hardcoded; never printed. The how-inline example does ZERO network and handles no token. |
| T-07-08 | Tampering | docs/consumers.md HTTP schema | mitigate | Docs point at `/openapi.json` as the single source of truth (rule 21) rather than re-specifying — no drift surface. |
</threat_model>

<verification>
- `bun run --cwd examples test -- --run how-inline-usage` passes (CONS-01 offline proof).
- `examples/ottolax-client.py` parses (`python -c "import ast; ast.parse(...)"`), is stdlib-only (no `import requests`), reads token from env.
- `docs/consumers.md` references `/openapi.json` and marks cross-repo + DEFERRED-LIVE follow-ups.
- No hardcoded bearer token in any example.
- `bun run --filter '*' test` does NOT regress.
</verification>

<success_criteria>
- CONS-01 PROVEN NOW: @geo/core imports and runs checkRobots + detectRendering offline (test green).
- CONS-02: stdlib Python client matches the verified HTTP contract; token from env.
- docs/consumers.md documents both consumers with /openapi.json as source of truth.
- (DEFERRED cross-repo) actual HOW + ottolax repo wiring — separate repo-scoped sessions (rule 20).
- (DEFERRED-LIVE) ottolax live round-trip against deployed geo-api.
</success_criteria>

<output>
Create `.planning/phases/07-cron-consumer-wiring/07-03-SUMMARY.md` when done.
</output>
