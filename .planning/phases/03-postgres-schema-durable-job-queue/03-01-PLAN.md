---
phase: 03-postgres-schema-durable-job-queue
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - packages/db/src/migrate.ts
  - packages/db/scripts/migrate.ts
  - packages/db/migrations/0001_create_audits.sql
  - packages/db/src/__tests__/migrate.test.ts
  - packages/db/src/__tests__/schema.test.ts
autonomous: true
requirements: [DATA-04, DATA-01]

must_haves:
  truths:
    - "Running migrations on a fresh DB creates the audits table with every required column"
    - "Running migrations twice is a no-op the second time (idempotent via schema_migrations)"
    - "migrate:status lists applied migration versions"
    - "Each migration file runs inside a transaction and rolls back on failure"
  artifacts:
    - path: "packages/db/migrations/0001_create_audits.sql"
      provides: "audits table + status CHECK constraint + updated_at trigger + partial queued index + url_hash index"
      contains: "CREATE TABLE audits"
    - path: "packages/db/src/migrate.ts"
      provides: "idempotent runMigrations + listApplied against schema_migrations"
      contains: "schema_migrations"
    - path: "packages/db/scripts/migrate.ts"
      provides: "bun run entrypoint: apply pending + status subcommand"
  key_links:
    - from: "packages/db/src/migrate.ts"
      to: "packages/db/migrations/*.sql"
      via: "readdir + sorted apply, each in sql.begin + tx.unsafe"
      pattern: "schema_migrations"
---

<objective>
Build the versioned migration system (DATA-04) and the first migration that creates the `audits` schema (DATA-01). A ~40-line idempotent runner records applied versions in `schema_migrations` and applies each pending `NNNN_<name>.sql` file inside its own transaction. No migration framework (per D-04).

Purpose: A repeatable, fresh-DB-safe schema is the foundation the DAL (plan 02) is built and tested against.
Output: `migrations/0001_create_audits.sql`, the runner, the `bun run migrate`/`migrate:status` entrypoint, and PGlite-backed tests proving idempotency and full column presence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-postgres-schema-durable-job-queue/03-CONTEXT.md
@.planning/phases/03-postgres-schema-durable-job-queue/03-RESEARCH.md
@.planning/phases/03-postgres-schema-durable-job-queue/03-VALIDATION.md
@packages/db/src/client.ts
@packages/db/src/__tests__/harness.ts

<interfaces>
<!-- audits table column contract (D-06/D-07) — the runner just executes the SQL; this is what 0001 must produce. -->
audits columns: id uuid PK default gen_random_uuid(); url text NOT NULL; normalized_url text NOT NULL;
url_hash text NOT NULL; status text NOT NULL default 'queued' CHECK status IN (queued,running,done,failed);
score int CHECK (score IS NULL OR 0..100); findings jsonb; error_code text; callback_url text;
attempts int NOT NULL default 0; locked_at timestamptz; lease_expires_at timestamptz;
created_at timestamptz NOT NULL default now(); updated_at timestamptz NOT NULL default now();
started_at timestamptz; finished_at timestamptz.
Indexes: partial index on (created_at) WHERE status='queued' (claim path); index on (url_hash) (dedup).
Trigger: set_updated_at() BEFORE UPDATE sets NEW.updated_at = now().
schema_migrations: version text PK, applied_at timestamptz default now().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration runner + scripts entrypoint</name>
  <files>packages/db/src/migrate.ts, packages/db/scripts/migrate.ts</files>
  <read_first>packages/db/src/client.ts, packages/db/src/__tests__/harness.ts, packages/db/package.json</read_first>
  <behavior>
    - runMigrations on a fresh DB creates schema_migrations then applies every NNNN_*.sql in sorted order
    - A second runMigrations call applies nothing (each version already recorded) — idempotent
    - listApplied returns the recorded versions in order
    - A failing migration file aborts its transaction and leaves schema_migrations unchanged for that version
  </behavior>
  <action>Create src/migrate.ts exporting runMigrations(db, migrationsDir) and listApplied(db) (per D-04). It must accept the query interface so the same logic runs against both the postgres.js sql instance (production) and the PGlite harness (tests) — parameterize on the executor rather than importing the singleton directly. Logic: CREATE TABLE IF NOT EXISTS schema_migrations (version text PRIMARY KEY, applied_at timestamptz DEFAULT now()); readdir the migrations dir, filter .sql, sort lexicographically; for each, derive version = filename without .sql; skip if already in schema_migrations; otherwise within a single transaction (sql.begin) run the raw file text via tx.unsafe(sqlText) THEN INSERT the version row. ANNOTATE clearly: tx.unsafe() is used ONLY for migration files we own — never with runtime/user input (T-03-UNSAFE, Pitfall 5). Create scripts/migrate.ts as the bun entrypoint wired by package.json (D-05): default = construct the real client (getSql), runMigrations against ./migrations, log applied versions; "status" arg = print listApplied. Resolve migrations dir relative to the package, not cwd.</action>
  <verify>
    <automated>bun run --cwd packages/db test -- --run src/__tests__/migrate.test.ts</automated>
  </verify>
  <done>runMigrations is idempotent and transactional; listApplied lists versions; scripts/migrate.ts runs apply + status; tests green against PGlite.</done>
</task>

<task type="auto">
  <name>Task 2: 0001_create_audits.sql + schema assertion test</name>
  <files>packages/db/migrations/0001_create_audits.sql, packages/db/src/__tests__/migrate.test.ts, packages/db/src/__tests__/schema.test.ts</files>
  <read_first>packages/db/src/migrate.ts, packages/db/src/__tests__/harness.ts, .planning/phases/03-postgres-schema-durable-job-queue/03-RESEARCH.md</read_first>
  <action>Write migrations/0001_create_audits.sql implementing the full audits table per D-06/D-07: all columns with exact types/defaults/constraints from the interfaces block (status CHECK constraint named audits_status_check; score range CHECK; attempts default 0; the timestamptz columns). Include the set_updated_at() plpgsql function + audits_updated_at BEFORE UPDATE trigger; the partial index idx_audits_queued ON audits (created_at) WHERE status='queued'; and idx_audits_url_hash ON audits (url_hash). Use gen_random_uuid() (built-in Postgres 13+, no pgcrypto). Write migrate.test.ts (idempotency: runMigrations twice on a fresh PGlite db is a no-op the second time, listApplied shows ["0001_create_audits"]) and schema.test.ts (DATA-01: after migrate, query information_schema.columns for table 'audits' and assert every required column name is present with expected type family; assert the status CHECK rejects an invalid status via a failing INSERT; assert score CHECK rejects 101 and accepts NULL). Both run against PGlite, no Docker.</action>
  <verify>
    <automated>bun run --cwd packages/db test -- --run</automated>
  </verify>
  <done>0001 migration applies clean on a fresh PGlite db; schema.test asserts all D-06 columns + status/score CHECK behavior; migrate.test asserts idempotency; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → DB | SQL files we own are executed raw via tx.unsafe — trusted only because they are committed, reviewed files, never runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-UNSAFE | Tampering | migrate.ts tx.unsafe() | mitigate | unsafe() scoped to migration files only; explicit code comment forbidding runtime input; all DAL queries (plan 02) use tagged templates |
| T-03-MIG | Tampering | runMigrations | mitigate | each file in a single sql.begin transaction → partial-apply rolls back; schema_migrations PK prevents double-apply |
| T-03-CHK | Tampering | 0001 status/score CHECK | mitigate | CHECK-constrained text status (not enum — additive-evolution safe per anti-pattern note); score 0..100 CHECK bounds the value |
</threat_model>

<verification>
- `bun run --cwd packages/db test -- --run` green (migrate idempotency + schema column/CHECK assertions) on PGlite, no Docker
- 0001 migration applies twice with no error; second apply is a no-op
</verification>

<success_criteria>
- Fresh-DB migrate yields the full audits schema with all required columns (DATA-01)
- Schema is versioned + repeatable + idempotent via schema_migrations (DATA-04)
- tx.unsafe is confined to trusted migration files
</success_criteria>

<output>
Create `.planning/phases/03-postgres-schema-durable-job-queue/03-01-SUMMARY.md` when done
</output>
