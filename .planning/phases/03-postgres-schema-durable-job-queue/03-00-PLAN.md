---
phase: 03-postgres-schema-durable-job-queue
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/db/tsup.config.ts
  - packages/db/vitest.config.ts
  - packages/db/src/client.ts
  - packages/db/src/index.ts
  - packages/db/src/__tests__/harness.ts
  - packages/db/src/__tests__/client.test.ts
  - .env.example
  - .gitignore
autonomous: true
requirements: [DATA-03]
user_setup: []

must_haves:
  truths:
    - "Importing @geo/db with DATABASE_URL unset throws a fail-fast error at startup"
    - "A PGlite in-process Postgres can be spun up fresh per test file with no Docker"
    - "DATABASE_URL never appears in any committed file; .env.example carries only a placeholder"
  artifacts:
    - path: "packages/db/package.json"
      provides: "@geo/db workspace package with postgres runtime dep + pglite/vitest dev deps"
      contains: "@geo/db"
    - path: "packages/db/src/client.ts"
      provides: "postgres.js sql singleton + DATABASE_URL fail-fast guard"
      contains: "DATABASE_URL"
    - path: "packages/db/src/__tests__/harness.ts"
      provides: "PGlite fresh-db factory + optional real-DATABASE_URL adapter"
      contains: "PGlite"
    - path: ".env.example"
      provides: "DATABASE_URL placeholder"
      contains: "DATABASE_URL"
  key_links:
    - from: "packages/db/src/client.ts"
      to: "process.env.DATABASE_URL"
      via: "fail-fast guard"
      pattern: "process\\.env\\.DATABASE_URL"
---

<objective>
Scaffold the `@geo/db` workspace package and the test harness everything downstream depends on. Stand up the postgres.js client with a fail-fast `DATABASE_URL` guard (DATA-03), and a PGlite-based test harness that gives every later plan a real in-process Postgres without Docker.

Purpose: Wave 0 removes the validation gaps recorded in 03-VALIDATION.md — no later task can be verified until the package, vitest config, and PGlite harness exist.
Output: A buildable, testable empty `@geo/db` package with a passing DATABASE_URL-guard unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-postgres-schema-durable-job-queue/03-CONTEXT.md
@.planning/phases/03-postgres-schema-durable-job-queue/03-RESEARCH.md
@.planning/phases/03-postgres-schema-durable-job-queue/03-VALIDATION.md

<interfaces>
<!-- Workspace conventions extracted from packages/fetch (mirror these exactly). -->

Root package.json: { "private": true, "workspaces": ["packages/*"] }

packages/fetch/package.json shape (mirror for @geo/db):
- "type": "module"
- dual exports: import → dist/index.js + dist/index.d.ts; require → dist/index.cjs + dist/index.d.cts
- "main": "./dist/index.cjs", "module": "./dist/index.js", "types": "./dist/index.d.ts"
- "files": ["dist"]
- scripts: { "build": "tsup", "test": "vitest" }

packages/fetch/tsconfig.json: target ES2022, lib [ES2023, DOM], types [node], module ESNext,
moduleResolution bundler, strict, noUncheckedIndexedAccess, skipLibCheck, ignoreDeprecations "6.0",
declaration + declarationMap + sourceMap, outDir ./dist, rootDir ./src, include ["src/**/*.ts"]

packages/fetch/tsup.config.ts: entry ["src/index.ts"], format ["esm","cjs"], dts, clean, sourcemap, target es2022

packages/fetch/vitest.config.ts: test.include ["src/__tests__/**/*.test.ts"], environment "node", watch false

Verified npm versions: postgres@3.4.9, @electric-sql/pglite@0.5.1, tsup@8.5.1, typescript@6.0.3,
vitest@4.1.8, @types/node@25.9.1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold @geo/db package files</name>
  <files>packages/db/package.json, packages/db/tsconfig.json, packages/db/tsup.config.ts, packages/db/vitest.config.ts, packages/db/src/index.ts</files>
  <read_first>packages/fetch/package.json, packages/fetch/tsconfig.json, packages/fetch/tsup.config.ts, packages/fetch/vitest.config.ts</read_first>
  <action>Create packages/db mirroring the @geo/fetch package layout (per D-01). package.json: name "@geo/db", version "0.1.0", type module, dual ESM/CJS exports + main/module/types/files exactly as @geo/fetch. scripts: build "tsup", test "vitest", and ADD migrate scripts wired to scripts/migrate.ts created in plan 01 — migrate "bun scripts/migrate.ts" and migrate:status "bun scripts/migrate.ts status" (per D-05). dependencies: "postgres": "3.4.9" (per D-02 — postgres.js, NOT Bun.sql), "@geo/core": "workspace:*" (for findings types reused in plan 02). devDependencies: "@electric-sql/pglite": "0.5.1" (real in-process WASM Postgres for Docker-free tests per 03-VALIDATION.md), "@types/node": "25.9.1", "tsup": "8.5.1", "typescript": "6.0.3", "vitest": "4.1.8". Copy tsconfig.json, tsup.config.ts, vitest.config.ts verbatim from @geo/fetch. Create src/index.ts as an empty re-export stub (export {} placeholder; plan 01/02 populate it). Run bun install at repo root to link the workspace.</action>
  <verify>
    <automated>bun install && bun run --cwd packages/db build && node -e "require('fs').statSync('packages/db/dist/index.js')"</automated>
  </verify>
  <done>packages/db builds with tsup producing dist/index.js + dist/index.cjs + dist/index.d.ts; @geo/db resolves as a workspace package.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: postgres.js client with DATABASE_URL fail-fast guard + .env scaffolding</name>
  <files>packages/db/src/client.ts, packages/db/src/__tests__/client.test.ts, .env.example, .gitignore</files>
  <read_first>packages/db/package.json, .gitignore</read_first>
  <behavior>
    - Importing/calling the client factory when process.env.DATABASE_URL is unset throws an Error whose message names DATABASE_URL (fail-fast, DATA-03)
    - When DATABASE_URL is set, the factory returns a postgres.js sql instance without throwing
    - The guard never logs or echoes the DATABASE_URL value (no leakage)
  </behavior>
  <action>Create src/client.ts exporting a lazy factory getSql() (NOT a module-top-level throw — a top-level throw would break test imports that never touch a real DB). getSql() reads process.env.DATABASE_URL; if falsy, throw new Error("@geo/db: DATABASE_URL is required but not set") per D-03 — do NOT include the value in any log line. On success construct postgres(url, { max: 10, idle_timeout: 20, connect_timeout: 10 }) and memoize the instance. Use postgres.js tagged-template API exclusively (mitigates SQL injection structurally — T-03-INJ). Add .env.example with a single line DATABASE_URL= (placeholder, no real value). Ensure .gitignore contains a line ignoring .env (add if absent) so the real value can never be committed (DATA-03). Write client.test.ts: a unit test (no DB) that deletes DATABASE_URL from env and asserts getSql() throws with a message matching /DATABASE_URL/; a second test that sets a dummy DATABASE_URL and asserts getSql() does not throw. These run without any Postgres.</action>
  <verify>
    <automated>bun run --cwd packages/db test -- --run src/__tests__/client.test.ts</automated>
  </verify>
  <done>client.test.ts passes: guard throws when DATABASE_URL unset (message names DATABASE_URL), constructs sql when set; .env.example has placeholder only; .gitignore ignores .env.</done>
</task>

<task type="auto">
  <name>Task 3: PGlite test harness (fresh in-process db) + optional real-DATABASE_URL adapter</name>
  <files>packages/db/src/__tests__/harness.ts</files>
  <read_first>packages/db/src/client.ts, .planning/phases/03-postgres-schema-durable-job-queue/03-VALIDATION.md</read_first>
  <action>Create harness.ts exporting two helpers downstream test files use. (1) makePgliteDb(): constructs a fresh in-memory PGlite instance (new PGlite() — no data dir, ephemeral per call) and returns an object exposing a query interface compatible with how the migration runner and DAL execute SQL — expose both a raw exec(sqlText: string) for migration files and a tagged/parameterized query path. Since the DAL is written against postgres.js, expose a thin adapter so plan-01/02 tests can run the same DAL/runner logic against PGlite; document in a header comment that PGlite is single-connection and therefore cannot prove true SKIP LOCKED concurrency (that proof is gated behind DATABASE_URL and completed in Phase 6 DEPLOY-04). (2) hasRealDb(): returns Boolean(process.env.DATABASE_URL); export a describeIfRealDb = process.env.DATABASE_URL ? describe : describe.skip helper so the concurrency test in plan 02 skips-with-reason when no real DB is present. Add a teardown helper to close the PGlite instance. NO fenced implementation here — the executor writes the adapter; keep it minimal and typed.</action>
  <verify>
    <automated>bun run --cwd packages/db test -- --run</automated>
  </verify>
  <done>harness.ts exports makePgliteDb, hasRealDb, describeIfRealDb, and a teardown helper; a smoke assertion (makePgliteDb runs "SELECT 1") passes under vitest with no Docker; existing client.test.ts still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | DATABASE_URL crosses from Coolify/shell env into the process; must never be committed or logged |
| filesystem → git | .env file with real credentials must never enter version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-LEAK | Information Disclosure | client.ts / .env | mitigate | DATABASE_URL env-only (D-03); .env.example placeholder; .gitignore ignores .env; guard error message names the var but NEVER its value; no log of the URL |
| T-03-INJ | Tampering | client.ts | mitigate | postgres.js tagged-template parameterization only; `unsafe()` deferred to migration runner (plan 01) over trusted files |
| T-03-SC | Tampering | bun install of postgres / pglite | accept | Both packages slopcheck [OK] in 03-RESEARCH Package Legitimacy Audit (postgres ~11yr, pglite official electric-sql); no [ASSUMED]/[SUS] packages → no install checkpoint required |
</threat_model>

<verification>
- `bun install && bun run --cwd packages/db build` succeeds (dual ESM/CJS dist emitted)
- `bun run --cwd packages/db test -- --run` green (client guard + PGlite smoke), no Docker required
- `git grep -n "DATABASE_URL=" -- ':!*.example'` returns no real value (placeholder only in .env.example)
</verification>

<success_criteria>
- @geo/db is a linked workspace package that builds and tests clean
- DATABASE_URL guard fails fast (DATA-03) and never leaks the value
- PGlite harness gives downstream plans a real in-process Postgres with no Docker
</success_criteria>

<output>
Create `.planning/phases/03-postgres-schema-durable-job-queue/03-00-SUMMARY.md` when done
</output>
