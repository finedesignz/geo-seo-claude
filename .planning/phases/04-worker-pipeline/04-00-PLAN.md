---
phase: 04-worker-pipeline
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/worker/package.json
  - packages/worker/tsup.config.ts
  - packages/worker/vitest.config.ts
  - packages/worker/src/index.ts
  - packages/worker/src/env.ts
  - packages/worker/src/types.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "@geo/worker is a workspace package that builds (tsup dual ESM+CJS+d.ts) and tests (vitest) like @geo/db"
    - "Worker fails fast at startup if ANTHROPIC_API_KEY or DATABASE_URL is missing"
    - "AnthropicMessagesClient injection seam exists so the SDK can be faked without vi.mock"
    - "Worker type surface conforms to @geo/db AuditDal and @geo/core Fetcher (tsc clean)"
  artifacts:
    - path: "packages/worker/package.json"
      provides: "@geo/worker workspace package + deps + bin"
      contains: "@geo/worker"
    - path: "packages/worker/src/env.ts"
      provides: "assertEnv fail-fast guard"
      exports: ["assertEnv"]
    - path: "packages/worker/src/types.ts"
      provides: "AnthropicMessagesClient + WorkerOptions injection seam"
      exports: ["AnthropicMessagesClient", "WorkerOptions"]
    - path: "packages/worker/src/index.ts"
      provides: "public barrel"
  key_links:
    - from: "packages/worker/src/types.ts"
      to: "@geo/db AuditDal + @geo/core Fetcher"
      via: "type imports"
      pattern: "from \"@geo/db\"|from \"@geo/core\""
---

<objective>
Scaffold the `@geo/worker` workspace package, mirroring `packages/db` exactly (tsup dual build, vitest, ESM `.js` import extensions), plus the two seams every later task depends on: the fail-fast env guard (`assertEnv`) and the constructor-injectable `AnthropicMessagesClient` interface. No business logic — this is the contract/skeleton wave.

Purpose: Waves 1 and 2 build against these contracts without exploring the codebase. The injection seam is what lets scorer/worker tests run with zero live API spend.
Output: A buildable, testable empty package with the env guard, the Anthropic client interface, the `WorkerOptions` shape, and a barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-worker-pipeline/04-CONTEXT.md
@.planning/phases/04-worker-pipeline/04-PATTERNS.md

<interfaces>
<!-- Contracts later waves consume. Use directly; do not re-explore. -->

From @geo/db (packages/db/src/dal.ts):
  export interface AuditDal {
    insertJob(input): Promise<AuditJob>;
    claimNextJob(leaseSecs?: number): Promise<AuditJob | null>;
    completeJob(id: string, leaseToken: string, score: number, findings: FindingsShape): Promise<boolean>;
    failJob(id: string, leaseToken: string, errorCode: string): Promise<boolean>;
    renewLease(id: string, leaseToken: string, secs: number): Promise<boolean>;
    reclaimExpired(maxAttempts?: number): Promise<number>;
    getJob / listJobs / findRecentByUrlHash ...
  }
  // AuditJob.leaseToken: string | null; AuditJob.findings: FindingsShape | null

From @geo/core (packages/core/src/types.ts):
  export type Fetcher = (url: string) => Promise<FetchResult>;
  // FetchResult { url; status; headers; body; redirectChain; error? }

From @anthropic-ai/sdk (Pattern 6, RESEARCH):
  AnthropicMessagesClient { messages: { create(params, options?): Promise<Anthropic.Message> } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package scaffold (package.json, tsup, vitest, barrel)</name>
  <read_first>
    packages/db/package.json (analog — copy structure verbatim, change name)
    packages/db/tsup.config.ts (analog)
    packages/db/vitest.config.ts (analog)
    packages/db/src/index.ts (barrel analog)
    .planning/phases/04-worker-pipeline/04-PATTERNS.md (exact package.json + tsup deltas)
  </read_first>
  <files>packages/worker/package.json, packages/worker/tsup.config.ts, packages/worker/vitest.config.ts, packages/worker/src/index.ts</files>
  <action>
    Create `packages/worker/package.json` as `@geo/worker` version 0.1.0, `type: module`, dual `exports` block + `main`/`module`/`types` pointing at `dist/`, `files: ["dist"]`, and a `bin: { "geo-worker": "./dist/main.js" }` for the Phase 6 entrypoint. Scripts: `build: tsup`, `test: vitest`. Dependencies: `@geo/core`, `@geo/fetch`, `@geo/db` as `workspace:*`, `@anthropic-ai/sdk` pinned `0.100.1`, `zod` at the workspace version already used elsewhere. devDependencies mirror db: `@electric-sql/pglite` 0.5.1, `@types/node` 25.9.1, `tsup` 8.5.1, `typescript` 6.0.3, `vitest` 4.1.8. Do NOT include `postgres`, `migrations`, or any migrate scripts.
    Create `tsup.config.ts` identical to db's but with `entry: ["src/index.ts", "src/main.ts"]` (main.ts is the bin entry, added in wave 2). Create `vitest.config.ts` verbatim from db (`include: ["src/__tests__/**/*.test.ts"]`, `environment: "node"`, `watch: false`).
    Create `src/index.ts` as a comment-headed barrel that re-exports (with `.js` extensions) `assertEnv` from `./env.js` and the public types from `./types.js`. Leave `runWorker`/`ScoringError` exports commented with a `// wave 1/2` marker so the file compiles now and is uncommented later. Run `bun install` from repo root so the workspace link resolves.
  </action>
  <verify>
    <automated>bun install ; bun run --cwd packages/worker build</automated>
  </verify>
  <done>`bun run --cwd packages/worker build` produces `dist/index.js`, `dist/index.cjs`, `dist/index.d.ts`. `package.json` name is `@geo/worker` with the three workspace deps + `@anthropic-ai/sdk` + `zod`.</done>
</task>

<task type="auto">
  <name>Task 2: Env guard + injection-seam types + conformance test</name>
  <read_first>
    packages/db/src/client.ts (assertDatabaseUrl / getSql fail-fast pattern, lines 21-34)
    packages/db/src/dal.ts (AuditDal interface this must type-conform to)
    packages/core/src/types.ts (Fetcher / FetchResult)
    .planning/phases/04-worker-pipeline/04-RESEARCH.md (Pattern 6 AnthropicMessagesClient; env example)
  </read_first>
  <files>packages/worker/src/env.ts, packages/worker/src/types.ts, packages/worker/src/__tests__/scaffold.test.ts</files>
  <action>
    `src/env.ts`: export `assertEnv(): void` mirroring the db `@geo/db: DATABASE_URL is required...` message format — throw if `process.env["ANTHROPIC_API_KEY"]` is unset (`@geo/worker: ANTHROPIC_API_KEY is required but not set. Set ANTHROPIC_API_KEY in your environment (Coolify env in Phase 6). Never commit a real API key.`) and again if `process.env["DATABASE_URL"]` is unset. Read keys via bracket notation (matches db). No side effects on import.
    `src/types.ts`: export `interface AnthropicMessagesClient` whose `messages.create(params: Anthropic.MessageCreateParamsNonStreaming, options?: Anthropic.RequestOptions): Promise<Anthropic.Message>` (import the SDK types via `import type Anthropic from "@anthropic-ai/sdk"`). Export `interface WorkerOptions` with the fields the bin reads (from PATTERNS main.ts): `concurrency`, `pollIntervalMs`, `leaseTtlSecs`, `reclaimIntervalMs`, `maxAttempts`, `scoringTimeoutMs`, `shutdownGraceMs`, `scoringModel` (all numbers except `scoringModel: string`), plus injectable `dal: AuditDal` (import from `@geo/db`) and `anthropic: AnthropicMessagesClient`. Re-export `Fetcher` type usage by importing from `@geo/core` to force the type-conformance link.
    Add `src/__tests__/scaffold.test.ts`: (a) `assertEnv` throws when ANTHROPIC_API_KEY deleted from a cloned env, throws when DATABASE_URL missing, passes when both set (mutate + restore `process.env` in try/finally); (b) a compile-time conformance assertion — declare a `const _dal: import("@geo/db").AuditDal = ...` cast and a `WorkerOptions` literal to prove the types line up (the test body can be a trivial `expect(true).toBe(true)` whose value is that the file type-checks).
  </action>
  <verify>
    <automated>bun run --cwd packages/worker test -- --run ; bunx tsc --noEmit -p packages/worker/tsconfig.json 2>nul || bun run --cwd packages/worker build</automated>
  </verify>
  <done>scaffold.test.ts passes: assertEnv throws on each missing var and passes when both present; the file type-checks against `@geo/db` `AuditDal` and `@geo/core` `Fetcher`; build stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → worker | ANTHROPIC_API_KEY / DATABASE_URL secrets enter here; must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-INFO | Information disclosure | ANTHROPIC_API_KEY / DATABASE_URL | mitigate | env-only read via `process.env`; `assertEnv` fail-fast at start; no defaults, no committed value; scaffold.test asserts the throw |
| T-04-SC | Tampering | npm installs (@anthropic-ai/sdk, zod) | accept | both `[OK]` in RESEARCH Package Legitimacy Audit (10M+/wk, 100M+/wk, official repos) — no [ASSUMED]/[SUS], no checkpoint required |
</threat_model>

<verification>
- `bun run --cwd packages/worker build` green (dual ESM+CJS+d.ts emitted).
- `bun run --cwd packages/worker test -- --run` green.
- Worker types conform to `@geo/db` `AuditDal` + `@geo/core` `Fetcher` (no `tsc` errors).
</verification>

<success_criteria>
@geo/worker scaffolds and builds like @geo/db; `assertEnv` fail-fast proven by test; `AnthropicMessagesClient` + `WorkerOptions` injection seams exist and type-conform to consumed contracts.
</success_criteria>

<output>
Create `.planning/phases/04-worker-pipeline/04-00-SUMMARY.md` when done.
</output>
