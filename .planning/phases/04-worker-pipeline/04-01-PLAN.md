---
phase: 04-worker-pipeline
plan: 01
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - packages/worker/src/scorer.ts
  - packages/worker/src/index.ts
  - packages/worker/src/__tests__/scorer.test.ts
autonomous: true
requirements: ["SCORE-01", "SCORE-02", "SCORE-03", "SCORE-04"]
must_haves:
  truths:
    - "Scoring is ONE structured messages.create call per job using forced tool-use record_geo_score"
    - "The dynamic input is the serialized @geo/core findings object, NOT raw HTML"
    - "The static rubric system block carries cache_control:{type:'ephemeral'} and usage cache fields are surfaced"
    - "tool_use.input is zod-validated to {score:int 0-100, findings}; the client is built with maxRetries:0"
    - "Every failure mode maps to a typed ScoringError with the correct code+retryable flag; no partial score path exists in the scorer"
  artifacts:
    - path: "packages/worker/src/scorer.ts"
      provides: "createScorer + ScoringError + GeoScoreSchema + GEO_SCORING_RUBRIC + classifyScoringError"
      exports: ["createScorer", "ScoringError", "GeoScoreSchema"]
      min_lines: 80
    - path: "packages/worker/src/__tests__/scorer.test.ts"
      provides: "SCORE-01..04 coverage with injected fake Anthropic client"
  key_links:
    - from: "packages/worker/src/scorer.ts"
      to: "AnthropicMessagesClient.messages.create"
      via: "constructor injection"
      pattern: "messages\\.create"
    - from: "packages/worker/src/scorer.ts"
      to: "zod GeoScoreSchema"
      via: "safeParse on tool_use.input"
      pattern: "GeoScoreSchema\\.safeParse"
---

<objective>
The scorer vertical slice: a single function that turns a deterministic `@geo/core` findings object into a validated `{score, findings}` via ONE forced-tool-use Anthropic call, with prompt caching on the static rubric, an AbortController timeout, and exhaustive typed-error classification. The Anthropic client is injected (`AnthropicMessagesClient`) so tests run with zero API spend.

Purpose: Covers SCORE-01 (single structured call, no claude -p), SCORE-02 (findings-not-HTML input), SCORE-03 (prompt cache proven via usage metadata), SCORE-04 (clean retryable failure, never a partial score).
Output: `createScorer(client, opts)` returning a `score(findings, signal?)` function; `ScoringError`; `GeoScoreSchema`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-worker-pipeline/04-CONTEXT.md
@.planning/phases/04-worker-pipeline/04-RESEARCH.md
@.planning/phases/04-worker-pipeline/04-PATTERNS.md

<interfaces>
From wave 0 (packages/worker/src/types.ts):
  interface AnthropicMessagesClient { messages: { create(params, options?): Promise<Anthropic.Message> } }

From @geo/db types.ts:
  interface FindingsShape { robots?; llmsTxt?; schemaTemplate?; structuredData?; citability?; rendering? }

SDK call shape (RESEARCH Pattern 1, verified @anthropic-ai/sdk 0.100.1):
  client.messages.create({
    model, max_tokens,
    system: [{ type:'text', text: GEO_SCORING_RUBRIC, cache_control:{type:'ephemeral'} }],
    tools: [{ name:'record_geo_score', description, input_schema:{...} }],
    tool_choice: { type:'tool', name:'record_geo_score' },
    messages: [{ role:'user', content: JSON.stringify(findings) }],
  }, { signal });
  // extract: response.content.find(b => b.type==='tool_use').input
  // cache proof: response.usage.cache_creation_input_tokens / cache_read_input_tokens

Error classes (@anthropic-ai/sdk): APIConnectionTimeoutError, APIUserAbortError,
  APIConnectionError, RateLimitError(429), InternalServerError(5xx incl 529), APIError
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scorer.ts — ScoringError, GeoScoreSchema, GEO_SCORING_RUBRIC, createScorer</name>
  <read_first>
    packages/fetch/src/safe-fetcher.ts (injected-dep + non-throwing classification analog)
    packages/worker/src/types.ts (AnthropicMessagesClient from wave 0)
    packages/db/src/types.ts (FindingsShape — the input type)
    .planning/phases/04-worker-pipeline/04-RESEARCH.md (Pattern 1, Pattern 2, classifyScoringError, ScoringError, maxRetries:0 pitfall, ≥2048-token rubric pitfall)
  </read_first>
  <behavior>
    - Builds exactly ONE messages.create call with tool_choice {type:'tool', name:'record_geo_score'}.
    - system[0] is the static rubric with cache_control:{type:'ephemeral'}; messages[0].content is JSON.stringify(findings).
    - Extracts the tool_use block via content.find(b => b.type==='tool_use'); missing block → ScoringError('SCORING_MALFORMED_OUTPUT', true).
    - zod GeoScoreSchema.safeParse(block.input); failure → ScoringError('SCORING_MALFORMED_OUTPUT', true).
    - AbortController fires at scoringTimeoutMs; classifyScoringError maps timeout/abort→SCORING_TIMEOUT(true), 429→SCORING_RATE_LIMITED(true), 5xx/529→SCORING_API_ERROR(true), connection→SCORING_API_ERROR(true).
    - Returns { score:number, findings:object, usage } on success only.
  </behavior>
  <action>
    Define `class ScoringError extends Error` with `code` (the 4-value union) and `retryable: boolean`, `name='ScoringError'` (RESEARCH ScoringError Type verbatim). Define `GeoScoreSchema` with zod: `score` = `z.number().int().min(0).max(100)`, `findings` = `z.record(z.string(), z.unknown())` (per-dimension sub-scores + rationale; do not over-constrain). Export the inferred `GeoScoreOutput` type. Define `GEO_SCORING_RUBRIC` as a substantial static string (full GEO scoring methodology + per-dimension rubric + tool-purpose description) — this is the cached prefix so it MUST be stable/static; write it as genuinely complete methodology rather than padding to a guessed token count (codex review: cache min is uncertain ~1024-2048; do NOT pad just to hit a threshold — assert SCORE-03 via real usage.cache_* metadata). Define the `record_geo_score` tool with `input_schema` matching the score contract (`score` integer 0-100, `findings` object, both required).
    `createScorer(client: AnthropicMessagesClient, opts: { model: string; timeoutMs: number })` returns `async score(findings: FindingsShape, externalSignal?: AbortSignal)`: build an `AbortController`, `setTimeout(() => ac.abort(), opts.timeoutMs)`, link `externalSignal` so a lease-loss abort also cancels (if `externalSignal?.aborted` abort immediately; else add an `abort` listener). Make the call with `{ signal: ac.signal }`; `clearTimeout` in `finally`. On any thrown error, run `classifyScoringError(err)` (RESEARCH Code Examples verbatim — instanceof chain ending at `APIError` with `status >= 500` → retryable) and rethrow a `ScoringError`. On success extract+validate as in <behavior> and return `{ score, findings: parsed.data.findings, usage: response.usage }`.
    IMPORTANT: this module does NOT construct `new Anthropic()` — the client is injected. The production client (built in wave 2 main.ts) is what sets `maxRetries: 0`; document that requirement in a header comment so wave 2 honors it.
    Uncomment the scorer exports in `src/index.ts` (`ScoringError`, `createScorer`, type `GeoScoreOutput`).
  </action>
  <verify>
    <automated>bun run --cwd packages/worker build</automated>
  </verify>
  <done>scorer.ts exports createScorer/ScoringError/GeoScoreSchema; build green; no `new Anthropic` in scorer.ts (`grep -v '^\s*//' packages/worker/src/scorer.ts | grep -c 'new Anthropic'` == 0).</done>
</task>

<task type="auto">
  <name>Task 2: scorer.test.ts — SCORE-01..04 with injected fake client</name>
  <read_first>
    packages/fetch/src/__tests__/safe-fetcher.test.ts (test structure + injected-dep mock analog)
    packages/worker/src/scorer.ts (the unit under test, from Task 1)
    .planning/phases/04-worker-pipeline/04-VALIDATION.md (per-task verification map rows 4-SCORE-01..04)
    .planning/phases/04-worker-pipeline/04-PATTERNS.md (scorer.test.ts analog — buildMockClient, real SDK error throws)
  </read_first>
  <action>
    Build a `buildMockClient(overrides)` returning `AnthropicMessagesClient` with `messages.create: vi.fn()`. Build a `buildMockResponse()` whose `content` includes a `{type:'tool_use', name:'record_geo_score', input:{score:72, findings:{...}}}` block and a `usage` object containing `input_tokens`, `output_tokens`, `cache_creation_input_tokens: 1234`, `cache_read_input_tokens: 0`. Throw REAL SDK error subclasses (`import Anthropic from "@anthropic-ai/sdk"`) for the error cases so the scorer's `instanceof` checks fire.
    Tests (each an assertion from VALIDATION):
    - SCORE-01: one happy call → `create` called exactly once; assert the call arg `tool_choice` deep-equals `{type:'tool', name:'record_geo_score'}` and `tools[0].name === 'record_geo_score'`.
    - SCORE-01: returned object has zod-validated `score === 72`.
    - SCORE-02: assert `messages[0].content === JSON.stringify(findingsInput)` and `messages[0].content` does NOT contain raw HTML; assert `system[0].text === GEO_SCORING_RUBRIC`-equivalent (the rubric constant, not findings).
    - SCORE-03: assert call arg `system[0].cache_control` deep-equals `{type:'ephemeral'}`; assert returned `usage.cache_creation_input_tokens` is present (mock first call) and a second test where the mock returns `cache_read_input_tokens > 0` surfaces it.
    - SCORE-04 timeout: mock rejects with `APIConnectionTimeoutError` (and a separate `APIUserAbortError` case) → `score()` rejects with `ScoringError` code `SCORING_TIMEOUT`, `retryable === true`.
    - SCORE-04 429: mock rejects `RateLimitError` → `SCORING_RATE_LIMITED`, retryable true.
    - SCORE-04 5xx/529: mock rejects `InternalServerError` → `SCORING_API_ERROR`, retryable true.
    - SCORE-04 missing tool_use: mock response with content `[{type:'text', text:'...'}]` → `SCORING_MALFORMED_OUTPUT`, retryable true (ROADMAP success criterion #4: malformed output is retryable; MAX_ATTEMPTS bounds it).
    - SCORE-04 zod-invalid: mock tool_use.input `{score: 150, findings:{}}` → `SCORING_MALFORMED_OUTPUT`, retryable true (ROADMAP success criterion #4: malformed output is retryable; MAX_ATTEMPTS bounds it).
    - SCORE-04 no-partial-score: across all error cases, the scorer never returns a value (always throws) — there is no `completeJob` here; this guarantees wave 2's "completeJob only on success".
  </action>
  <verify>
    <automated>bun run --cwd packages/worker test -- --run</automated>
  </verify>
  <done>All scorer tests green. `messages.create` call-count is exactly 1 per `score()` invocation. Each of the 4 SCORE-04 error classes maps to the asserted code+retryable. Missing-tool_use and zod-invalid both yield retryable SCORING_MALFORMED_OUTPUT (criterion #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API → worker | LLM-generated `tool_use.input` is untrusted output crossing back into the worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-LLM | Tampering | scorer.ts tool_use.input | mitigate | zod `GeoScoreSchema.safeParse` rejects any output not matching `{score:int 0-100, findings}`; invalid → `SCORING_MALFORMED_OUTPUT` (retryable, bounded by MAX_ATTEMPTS); asserted by SCORE-04 zod-invalid test |
| T-04-RETRY | Denial of service | Anthropic client retry storm vs timeout | mitigate | production client built `maxRetries:0` (wave 2); per-call AbortController owns the bound; documented in scorer header (RESEARCH Pitfall 1) |
| T-04-INFO | Information disclosure | ANTHROPIC_API_KEY | mitigate | key never referenced in scorer (client injected); env-only, covered by wave 0 assertEnv |
</threat_model>

<verification>
- `bun run --cwd packages/worker test -- --run` green (scorer suite).
- Exactly one `messages.create` per `score()`; forced tool-use shape asserted.
- `cache_control:{type:'ephemeral'}` on system block asserted; usage cache fields surfaced.
- Findings (not HTML) is the dynamic input; rubric is the static cached prefix.
- All 4 SCORE-04 error classes map to the correct code+retryable; scorer never returns on any error path.
</verification>

<success_criteria>
SCORE-01..04 satisfied: single structured forced-tool-use call, findings-not-HTML input, prompt-cache wiring proven via usage metadata, exhaustive typed-error classification with no partial-score path — all under an injected, mockable Anthropic client.
</success_criteria>

<output>
Create `.planning/phases/04-worker-pipeline/04-01-SUMMARY.md` when done.
</output>
