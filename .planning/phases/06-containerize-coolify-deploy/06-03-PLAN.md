---
phase: 06-containerize-coolify-deploy
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - .planning/phases/06-containerize-coolify-deploy/DEPLOY-RECORD.md
autonomous: false
requirements: [DEPLOY-04]
must_haves:
  truths:
    - "The image is deployed live on Coolify as an API resource + a worker resource off one image"
    - "Migrations are applied against the Coolify Postgres (advisory-locked runner)"
    - "scripts/deploy-verify.sh passes live: healthz 200, docs reachable, unauth POST = 401, authed audit round-trip reaches done with numeric score + findings"
    - "Coolify worker stop grace is >= SHUTDOWN_GRACE_MS (redeploy loses no jobs)"
  artifacts:
    - path: ".planning/phases/06-containerize-coolify-deploy/DEPLOY-RECORD.md"
      provides: "Captured app UUIDs, deploy outcome, smoke-test transcript / deferral note"
  key_links:
    - from: "Coolify API resource"
      to: "Coolify Postgres"
      via: "DATABASE_URL env"
      pattern: "DATABASE_URL"
---

<objective>
Deploy the image live on Coolify and run the acceptance smoke (DEPLOY-04 + ROADMAP success criteria #2 and #4). This task depends on the HUMAN GATE (operator provisions the Coolify app + Postgres + enters secrets + wires two resources — see CONTEXT.md "Human Gate" and docs/deploy.md).

Purpose: Complete the phase with a verified live deploy, OR — if the human gate is not yet satisfied — deliver a clean, explicitly-marked deferral so the phase is NOT blocked on operator provisioning (the artifacts from plans 01/02 stand on their own; the live deploy becomes a Phase-7 precondition).
Output: `DEPLOY-RECORD.md` with the deploy outcome (UUIDs + smoke transcript) or a deferral note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-containerize-coolify-deploy/06-CONTEXT.md
@.planning/phases/06-containerize-coolify-deploy/06-RESEARCH.md
@docs/deploy.md
@scripts/deploy-verify.sh

<interfaces>
Coolify base: https://coolify.titaniumlabs.us  (server 46.224.61.233; API token in ~/.claude/secrets/services.json)
Deploy trigger: POST /api/v1/deploy?uuid=<app-uuid>
Smoke: scripts/deploy-verify.sh with GEO_API_BASE + GEO_API_TOKEN + AUDIT_URL set.
Human Gate (CONTEXT.md): provision app+Postgres, enter secrets, wire 2 resources + per-resource start cmd + health, stop grace >= SHUTDOWN_GRACE_MS.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Plans 01/02 produced the Dockerfile, .dockerignore, .env.example, worker heartbeat + healthcheck, deploy-verify script, and the docs/deploy.md runbook. The image is ready to deploy.</what-built>
  <decision>Is the Coolify geo-api app + Postgres provisioned, with the two resources (API + worker) wired and env secrets entered, per docs/deploy.md "Human Gate"?</decision>
  <context>
    Provisioning a NEW Coolify app + Postgres and entering secrets in the Coolify UI is operator-owned (no API path). The executor CAN trigger a deploy via the Coolify API and run the smoke IF the app UUID + a GEO_API_KEYS token + the live base URL exist. If they do NOT exist yet, the live deploy is deferred — it does not block the phase.
  </context>
  <options>
    <option id="gate-ready">
      <name>Human gate satisfied — proceed to live deploy</name>
      <pros>Phase fully completes with a verified live round-trip (DEPLOY-04, success criteria #2/#4)</pros>
      <cons>Requires the operator to have provisioned Coolify resources + secrets first</cons>
    </option>
    <option id="gate-deferred">
      <name>Human gate not yet satisfied — record deferral</name>
      <pros>Phase is not blocked; all code/doc artifacts ship; live deploy becomes a Phase-7 precondition</pros>
      <cons>DEPLOY-04 live verification deferred to operator</cons>
    </option>
  </options>
  <resume-signal>Reply "gate-ready: APP_UUID=<uuid> BASE_URL=<https://...> TOKEN=<bearer>" OR "gate-deferred"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Deploy + live smoke (or record deferral)</name>
  <files>.planning/phases/06-containerize-coolify-deploy/DEPLOY-RECORD.md</files>
  <action>
    Branch on the checkpoint outcome.
    IF gate-ready: trigger the Coolify deploy `POST https://coolify.titaniumlabs.us/api/v1/deploy?uuid=<APP_UUID>` (token from ~/.claude/secrets/services.json), poll deploy status until the build+deploy completes, then confirm the Pre-deployment migrate command ran (check deploy logs for the migrate runner / `schema_migrations`). Run `GEO_API_BASE=<BASE_URL> GEO_API_TOKEN=<TOKEN> AUDIT_URL=https://example.com bash scripts/deploy-verify.sh`. Capture the full transcript. Confirm the worker resource came up healthy (heartbeat) and that the worker stop grace ≥ SHUTDOWN_GRACE_MS is configured (success criterion #4). Write DEPLOY-RECORD.md with: app UUIDs, deploy id/status, migrate confirmation, the smoke transcript (healthz 200 → docs 200 → unauth 401 → authed job_id → poll-to-done with score+findings).
    IF gate-deferred: write DEPLOY-RECORD.md stating the live deploy is deferred pending operator provisioning, list exactly what the operator must do (cross-reference docs/deploy.md Human Gate), and note that all code/doc artifacts (plans 01/02) are complete and locally validated. Mark DEPLOY-04 live verification as a Phase-7 precondition. Do NOT fabricate a smoke transcript.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-containerize-coolify-deploy/DEPLOY-RECORD.md &amp;&amp; grep -qiE 'status.*done|deferred' .planning/phases/06-containerize-coolify-deploy/DEPLOY-RECORD.md</automated>
    <human-check>If gate-ready: the deploy-verify transcript shows healthz=200, unauth POST=401, and a GET /audit/{id} reaching "done" with a numeric score + findings.</human-check>
  </verify>
  <done>
    Either: live deploy succeeded and deploy-verify.sh passed end-to-end (DEPLOY-04 ✓), recorded in DEPLOY-RECORD.md. Or: a clean, explicit deferral is recorded with the operator action list — phase not blocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → Coolify API | deploy trigger with operator token; read-only beyond deploy |
| live API ← internet | bearer-gated; smoke verifies 401 on unauth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information disclosure | deploy logs | mitigate | env.ts names vars, never echoes values; verify no secret printed in captured transcript before committing DEPLOY-RECORD.md |
| T-06-09 | Tampering | smoke POST against live DB | accept | only inserts a benign audit job for a public URL; non-destructive |
</threat_model>

<verification>
- DEPLOY-RECORD.md exists with either a passing smoke transcript or an explicit deferral + operator action list.
- No secret value present in DEPLOY-RECORD.md.
</verification>

<success_criteria>
Live deploy verified (DEPLOY-04 + success criteria #2/#4) OR a clean deferral that keeps the phase unblocked and hands the live step to the operator / Phase-7 precondition.
</success_criteria>

<output>
Create `.planning/phases/06-containerize-coolify-deploy/06-03-SUMMARY.md` when done.
</output>
