#!/usr/bin/env node
// @geo/worker — CLI entrypoint (wave 2 fills this in)
// Placeholder so tsup can emit dist/main.js for the bin field.

// wave 2: assertEnv(), parse argv, build WorkerOptions, runWorker(opts)
